# 现金流计算器（finance-ficalc）升级方案

> 状态：已定案，待「开工」逐步落地。
> 参考：vault `3-项目/31-活跃项目/ob插件开发_finance-block/前期规划/功能扩展设计-四大洞察模块.md` 的「人生现金流模拟器」与「模块 D 人生事件 what-if」设计思想（仅借鉴思想，非照抄）。
> 关联记忆：`I:/project/finance-block/.workbuddy/memory/2026-08-01.md`

---

## 0. 背景与目标

**现状断层**：`finance-ficalc` 只有一个同质化的 `principal`（生息本金），假设整体按 `rate` 复利；而 `finance-assets` 是一堆**异构账户**（股票会涨、车会折旧、房不产被动收入、现金几乎不生息、房贷每月要还）。两者无法联动，因为缺的不是数据管道，而是一层**资产现金流行为语义分类**。

**目标（三件事，实为同一次架构升级）**：
1. 改名：财务自由目标计算器 → 现金流计算器（仅显示层）。
2. 资产联动：把 `finance-assets` 的真实数据按现金流行为分桶，喂进引擎。
3. 事件模拟器：用户可配置人生事件，结果直接反映在现金流曲线上。

**核心洞察**：**净资产 ≠ 生息本金**。只有生息类资产供养退休引擎；折旧固定资产、自住房只进净资产曲线、不供养退休。

---

## 1. 需求 1：改名

只改 i18n 显示层，**代码块围栏 `finance-ficalc` 保持不变**（改围栏会让存量笔记全部失效）。

| 键 | 现值 | 改为 |
|---|---|---|
| `ficalc.title` | 财务自由目标计算器 | 现金流计算器 |
| `block.finance-ficalc` | 财务自由计算器 | 现金流计算器 |
| `block.finance-ficalc.desc` | 给定利率/本金/花费… | 投影长期资产与现金流，模拟人生事件冲击 |
| 对应 en 三键 | 同步 | Cash Flow Simulator … |

「财务自由达成」不删除，降级为曲线上的一个**里程碑标记**（被动收入余量持续 ≥ 0 的拐点）。

---

## 2. 需求 2：资产联动与分桶模型

### 2.1 分桶机制
在 `AccountDef` 加可选字段 `cashflowRole: 'growth' | 'cash' | 'fixed' | 'rental'`，在账户管理弹窗设一次、全局单一真源；缺省按 `valuation`/`class` 智能推断，可覆盖。

### 2.2 分桶映射（现金流引擎的桶）

| 资产类型 | 桶 | 现金流行为 |
|---|---|---|
| 股票 / 基金 / 理财 | 生息本金 P | 按 `rate` 复利，退休后支取 |
| 存款 / 货基 | 生息本金·低息档 | 按现金收益率计息 |
| 现金 / 活期 | 应急金缓冲 + 余额并入 P | 见 2.3 |
| 车 / 电子（折旧） | 非生息净资产 | 按 `currentValue` 递减，不供养退休 |
| 自住房 | 非生息净资产 | 可设升值率，不产被动收入 |
| 出租房 | 现金流资产 | 租金作正现金流，可经事件变现注入 P |
| 房贷 / 车贷 | 负债 | 抵减净资产，月供作负现金流 |

### 2.3 现金处理（已确认）
先留 `N` 个月支出作**应急金缓冲**（不增长），余额按现金收益率（默认 1.5%）并入 P。避免用股票 `r` 高估现金收益。

### 2.4 折旧固定资产的处置
车/电子**不进 P**，只进净资产曲线按现有折旧引擎递减；其唯一进入现金流的方式是**事件**（如「5 年后换车 −20 万」「晚年卖车变现注入 P」）。所有固定资产变动统一走事件，不单独建模。

### 2.5 房产处理
- **自住房**：进净资产、可设升值率画曲线，不产被动收入、不供养退休；月供/持有成本经负债与事件体现。
- **出租房**：租金算正现金流。
- **晚年卖房养老**：用「某年卖房」事件把该资产从净资产桶转进 P 桶。

---

## 3. 引擎升级：完整生命周期曲线

### 3.1 两段合成
- **积累期**（今年 → 退休）：P 按 `rate` 复利 + 每年净储蓄逐年叠加。
- **支取期**（退休 → 终）：沿用现有 `projectPortfolio` / 蒙特卡洛。
- 全程叠加通胀、收入增长、以及事件增量。

### 3.2 双线定义（已确认：净资产 + 余量）
渲染两条曲线，语义清晰、可标里程碑：
- **曲线① 净资产水位线（Net Worth）**：各年总资产 − 总负债；折旧资产按 `currentValue` 递减；负债按剩余本金逐年递减。
- **曲线② 可持续现金流余量（Safe Cashflow）**：当年被动收入（生息资产 × rate + 租金）− 当年支出（含事件增量）。
- **财务自由里程碑** = 曲线②在某年后**持续 ≥ 0** 的拐点。

### 3.3 关键参数
新增代码块参数：`当前年龄` / `退休年龄` / `收入增长率` / `通胀率` / `投资回报率` / `年储蓄`（见 3.4）。通胀与增长、回报拆为独立参数，引擎统一走 real-rate 口径，避免积累期/支取期通胀打架。

### 3.4 年储蓄来源（已确认）
缺省从 `fin-beancount` **近 12 月净流入自动派生**；UI 提供手填框，手动填写即覆盖派生值。

### 3.5 蒙特卡洛（已确认）
降级为**可选置信带**：主线用确定性中值曲线（清晰），蒙特卡洛作可开关的 p10/p90 阴影带，避免随机性掩盖事件效果。

---

## 4. 需求 3：事件模拟器

### 4.1 统一增量模型
每个事件编译成「某年起的时间线增量 `deltas`」之一或组合：

| 字段 | 含义 | 举例 |
|---|---|---|
| `oneOff` | 仅当年一次性现金流 | 遗产 +50 万、装修 −20 万 |
| `deltaIncome` / `deltaSpend` | 从某年起持续 N 年的年收支变化 | 生娃 +3 万/年 ×18、换工作 −5 万/年 |
| `deltaPrincipal` | 一次性改变生息本金 P | 卖房 +300 万注入 P |
| `deltaFixed` / `deltaLiability` | 改变非生息资产 / 负债 | 买房：现金 −100、固定 +300、负债 +200 |
| `rateShift` | 从某年起切换收益率 | 退休后 r 从 6% 转 3% |

### 4.2 事件类型 v1（四类全做）
一次性收支 / 持续收支变化 / 资产·负债变动 / 参数切换。

### 4.3 时间坐标（已确认）
UI 同时展示「目标年龄 + 绝对年份」（如 28 岁 (2026)）双框联动；存储以 `atAge` 为主（相对当前年推算绝对年，跨年稳定），也允许直接填 `atYear`。引擎内部统一转绝对年。

### 4.4 存储与消费
`finance-config.json` 新增 `events[]`，配套「事件管理」弹窗；现金流块经 scenario/开关消费。符合项目「单一 config 文件」约定。

### 4.5 渲染与纯函数
引擎逐年 walk 时间轴累计叠加所有到期增量，产「基准 vs 含事件」双曲线 + 事件年标记点。逻辑抽纯函数 `projectLifeCashflow(baseline, events[])`，符合项目「视图块逻辑一律抽纯函数」约定。

---

## 5. 决策汇总（全部已确认）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 分桶机制 | `AccountDef.cashflowRole` 字段 + 智能推断可覆盖 |
| 2 | 现金处理 | 应急金缓冲 + 余额按 1.5% 低息并入 P |
| 3 | 事件存储 | `config.events[]` + 管理弹窗 |
| 4 | 事件类型 v1 | 四类全做 |
| 5 | 核心曲线 | 双线：净资产水位 + 可持续现金流余量 |
| 6 | 年储蓄来源 | 账本近 12 月自动派生 + 可手填覆盖 |
| 7 | 事件时间坐标 | 目标年龄 + 绝对年份双显，存 atAge/atYear |
| 8 | 存量块兼容 | 开发阶段不保证兼容，完成后统一处理 |

---

## 6. 分期落地与改动落点

**阶段 1 — 改名 + 引擎补积累期（仍手填参数）**
- `i18n/zh.ts` + `en.ts`：4 键（标题→现金流计算器，围栏不变）
- `engine/fiCalc.ts`：新增 `projectLifeCashflow` 纯函数 + 单测
- `codeblock/ficalc.ts`：渲染生命周期双曲线 + 里程碑
- `codeBlockDefs.ts`：补 当前年龄 / 退休年龄 / 收入增长率 / 通胀率 / 投资回报率 / 年储蓄

**阶段 2 — 资产接入**
- `types/index.ts`：加 `cashflowRole`
- `AccountManagerModal.ts`：字段 UI
- 新 `assetBuckets.ts`（或扩展 `networth.ts`）：分桶汇总
- `ficalc.ts`：`src: actual` 分支 + 应急金/现金利率参数；无 assets 时回退手填

**阶段 3 — 事件模拟器**
- `types/index.ts`：`EventDef`
- `config/defaults.ts`：events 默认结构
- 新 `ui/EventManagerModal.ts`：事件管理弹窗
- `fiCalc.ts`：事件叠加进 `projectLifeCashflow`
- `ficalc.ts`：基准 vs 含事件双曲线 + 时间线标记
- `main.ts`：注入 `openEventModal`

每阶段独立可验证；三处同步约定（`CodeBlockParamDef` → `blockProvider.ts` 的 `ParamDef` → `main.ts` 映射）勿漏。

---

## 7. 开放项（落地时再定，不影响方案）
- 应急金月数 `N` 的默认值与可调性。
- 出租房租金、负债月供在「账本派生」与「assets 字段」间的取数优先级。
- 多币种：分桶汇总沿用现有 `convertToBase` 换算到基准币种，非基准币种资产处理方式同上。
- 存量块迁移工具（阶段 3 后视需要补）。

---

## 8. beancount 账本合规约束（硬约束）

账本源文本（`fin-beancount` 代码块）须始终符合 beancount 语法，未来可能做导出功能，因此本方案对账本的改动边界如下：

- **账户定义与分析维度不进账本**：`cashflowRole` 等现金流角色加在 `finance-config.json` 的 `AccountDef`（JSON，见 `types/index.ts:46`），不写入 beancount 的 `open` 指令；账户名与账本账户名一一对应即可。
- **事件 / 模拟增量是 what-if，绝不写回账本**：事件仅存 `config.events[]`，用于引擎投影；未来导出 beancount 时只含真实入账交易，不含任何模拟事件。
- **账本只读派生**：年储蓄派生、资产市值读取均只读 `indexer` 交易流与余额，绝不修改账本源文本。
- **若未来需在账本体现分析元数据**：只能作为 beancount `open` 指令的合法 metadata（`key: value` 形式，beancount 原生支持任意 metadata），不得引入自定义指令或破坏语法；导出器据此生成。
