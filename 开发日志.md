# 财务块 Finance Block · 开发日志

> 记录截至 **2026-07-31** 的已开发功能全貌。本文档只写「代码里真实存在的能力」，
> 未落地的构想不写入。所有条目均可在 `src/` 下追溯到对应文件。

- 插件 ID：`finance-block`　版本：`0.1.0`　最低 Obsidian 版本：`1.12.7`
- 定位：以 ` ```fin-beancount ` 代码块为**唯一数据真源**的个人财务 OS，纯文本存 vault、可双链、可进 Git
- 源码规模：`src/` 下 53 个 TypeScript 文件，`styles.css` 605 行
- 测试：8 个测试文件 / 80 个用例，`npx vitest run` 全绿

---

## 一、数据模型：一切从 fin-beancount 开始

### 1.1 语法与解析器

实现文件：`src/parser/finBeancount.ts`（自研轻量复式解析器，零依赖）

支持的语法：

```fin-beancount
; 全行注释
2026-07-29 * 午餐 牛肉面        ; 行内注释
  现金        -3500              ; 金额单位：分
  费用:餐饮    3500
  type: 餐饮                     ; 元数据行
  owner: 自己

2026-07-30 ! 待确认转账          ; ! = pending 标记
  银行卡      -10000 USD         ; 可选币种后缀
  支付宝       10000 USD
^t-20260730120000                ; 入账后由 poster 附加的块引用 ID
```

解析规则（逐条已实现）：

- **日期行**：`YYYY-MM-DD [* | !] [摘要]`，正则 `DATE_RE`
- **分录行（leg）**：缩进 + 账户名 + 整数金额（分）+ 可选三字母币种，账户名支持中文、`:`、`/`、`-`
- **元数据行**：缩进 + `key: value`，key 支持 Unicode（中文键名可用）
- **零和校验**：一笔交易所有 leg 求和必须为 0，否则产出 `ParseError`（含 1-based 行号 + 差额分数）
- **无分录检测**：只有日期行没有 leg 时同样报错
- **保留键提取**：`type` → `Transaction.txnType`，`owner` → `Transaction.owner`，其余元数据落入 `fields`（可筛选）
- **块引用 ID 捕获**：`^t-` 行不作为交易数据，但其真实值会成为 `Transaction.id`；草稿态没有该行时退化为合成 id
- **边界处理**：空行 / 新日期行结束当前交易；`;` 开头整行忽略；无法识别的行产出定位错误

### 1.2 领域类型

实现文件：`src/types/index.ts`

- **金额约定**：`AmountInCents`，一律以整数「分」存储，全链路避免浮点误差
- `Transaction` / `BeancountLeg`：交易与分录
- `AccountClass`：五大类 `asset / liability / equity / income / expense`，账户强制归属其一，使「净资产 = 资产 − 负债」成为结构不变量
- `AccountDef`：账户定义，含会计增强字段 `accrued`（应计标记）、`sinkingFund`（专项储蓄池）、`depreciation`（折旧定义）
- `TransactionTypeDef`：交易类型 + 收支方向 + 自定义字段
- `CurrencyDef` / `BudgetDef` / `FICalcConfig` / `FinanceConfig`

---

## 二、账本与入账链路

### 2.1 单一账本文件模型

- 账本是**一个 md 文件**（默认 `账本/账本.md`），文件内**只保留一个** `fin-beancount` 块
- 每笔入账追加进这个块的底部，而不是新建块（否则账本会散落成多个块）
- 实现：`src/ledger/ledgerFile.ts` 的 `appendEntryToLedgerBlock`（5 个单测覆盖）

### 2.2 入账（Poster）

实现文件：`src/ledger/poster.ts`

草稿 → 账本的完整链路：

1. 解析代码块，校验通过才继续；已含 `^t-` 的块直接拒绝（防重复入账）
2. 生成块引用 ID `^t-YYYYMMDDHHmmss`（精确到秒）
3. 调 `appendEntryToLedgerBlock` 追加进账本唯一的 fin-beancount 块
4. 把原笔记里的 fin-beancount 块**原位替换**为一个 ` ```finance-log ` 块（携带 `id: ^t-xxx`）
   —— 既保留「此地记过一笔」的痕迹，又能就地展示该笔明细
5. 触发索引增量更新

还实现了 `batchPostTransactions`：一个块内多笔草稿一键各自独立入账，**从后往前**处理避免位置偏移，结果再反转回原始顺序。

### 2.3 周期结转（Closing）

实现文件：`src/ledger/closing.ts`　入口：命令面板「周期结转」

- `calculateBalances`：从已入账交易汇总各账户余额，支持起止日期过滤
- `calculateNetWorth`：资产 − 负债
- 周期范围计算：`week / month / quarter / year / custom` 五种，均已实现
- 生成结转分录：余额**取反**（清零旧账）+ 轧差到 `equity:结转`，整笔零和，追加进账本

### 2.4 汇总结转 / 账本轮换（Rollover）

实现文件：`src/ledger/rollover.ts`　入口：账本 fin-beancount 块上的「汇总结转」按钮 → `RolloverModal`

解决「单一账本越滚越大」的问题，链路是：

1. 计算截至截止日的各账户期末余额
2. 生成新账本的「期初结转 · 余额承接」分录（余额**正向**铺账，与 closing 的取反相反），轧差到 `equity:期初`
3. 创建新账本文件（标题 + 单个 fin-beancount 块）
4. 更新设置：`ledgerPath` 指向新账本，旧账本推入 `archiveLedgers`
5. 全量重建索引，所有视图即时更新

关键约定：**只结转余额不搬明细**（旧文件留全部明细作归档）；旧文件不重命名（笔记里 `finance-log id=` 按 ID 查，不受改名影响）；草稿不随轮换丢失。

### 2.5 索引器（Indexer）

实现文件：`src/ledger/indexer.ts`　**2026-07-31 改造为纯内存 + 待入账筛查可关**

- 启动扫描范围：
  - **账本文件**（激活账本 + 全部归档账本，视为已入账）——**始终**直接按路径读取，不遍历全库
  - **笔记中的草稿**（其余 md 文件里的 `fin-beancount` 块）——**仅当「待入账筛查」开启时**才扫描；默认关，此时启动零全库遍历
- 路径规范化：用户在 Windows 上常把路径写成 `账本\账本.md`，统一用 `normalizePath` 归一，避免账本被当草稿重复索引
- 待入账筛查范围：`DraftScanConfig { enabled, folders }`，由 `main.ts` 以 getter 注入（实时读取设置）；开启且 `folders` 非空时只扫命中的文件夹（前缀匹配，留空=全库）
- 开关即时生效：设置页切换会触发 `indexer.fullScan()` 重建索引——开=按范围扫草稿，关=清空草稿只读账本
- 增删改事件驱动增量更新：`indexFile` / `updateFile` / `removeFile`，均为幂等
- 对外查询：`getAllTransactions` / `getPostedTransactions` / `getStats`
- 一次性清理：init 时自动删除旧版本遗留在 vault 里的 `finance-index.json`

---

## 三、代码块体系

### 3.1 已注册的 6 种代码块

| 语言 | 角色 | 参数 |
|---|---|---|
| `fin-beancount` | 存储层·数据真源 | 模板模式：date / narration / amount / fromAccount / toAccount / txnType / owner |
| `finance-log` | 流水视图 | 全部选填：`date`（起始日，默认今天）、`day`（从起始日往前数 N 天，默认 30）、`amount`（金额范围表达式）、`account`（账户）、`type`（交易类型）、`owner`（归属维度）、`id`（块引用精确查询，`;` 分隔多个） |
| `finance-ficalc` | 财务自由计算器 | `rate`（年利率 %）、`principal`（本金，万）、`spend`（年花费，万）、`useActualSpend` |
| `finance-fi` | 财务自由进度仪表 | `rate` |
| `finance-budget` | 预算执行率 | `type`（按交易类型筛选预算计划） |
| `finance-heatmap` | 支出热力图 | `weeks`（最近 N 周，默认 12，上限 52） |

定义收敛在单一数据源 `src/codeBlockDefs.ts`：主弹窗卡片列表与参数表单都从这里读，新增代码块只需追加一条。
生成器 `buildCodeBlock` 支持两种模式——模板模式（`{{key}}` 占位替换）与键值模式（`key: value` 行）。

参数定义上有两个跨弹窗生效的开关：

- `optionsFrom: 'accounts' | 'transactionTypes' | 'owners'` —— `type: 'select'` 时从 vault 配置**实时**取下拉选项（取不到再回落静态 `options`）。取代了此前「按 `p.key` 猜数据源」的隐式约定，键名与数据源解耦。
- `autoToday` —— `type: 'date'` 时是否自动填今天。记账场景要（fin-beancount），筛选场景**必须留空**，否则「最近 N 天」会被写死成插入当天。

### 3.2 fin-beancount 块的渲染与交互

实现文件：`src/codeblock/registry.ts`

统一视觉骨架：`.finance-block` 根 + `.fb-head`（图标 chip + 标题 + 状态 pill）+ `.bc-*` 内容段。

**草案态**（源码不含 `^t-`）：

- 顶部状态 pill 显示「草稿」
- 工具栏：`添加记录`（追加分录到当前块的草稿区，**不写账本**——写账本会导致 finance-log 把草稿和已入账两笔都索引出来）
- 工具栏：`入账`；块内多笔时额外出现 `批量入账（N）`
- 底部零和校验脚注

**已入账态**：

- 当前账本内的块显示「当前账本」pill
- 每张交易卡片右上角有复制按钮，复制的是**块引用行**（`^t-xxxx`），供 `finance-log` 的 `id:` 参数精准查询单笔
- 若为当前账本，工具栏出现 `汇总结转` 按钮
- 底部渲染**账本流转链**：`旧账本 → 当前账本 → 新账本`，高亮当前所属；无任何承接关系时不渲染

**校验错误**：错误优先渲染，逐行给出 `⚠ 行号 + 说明`；已入账块出错时额外提示「已入账内容被改动」。

**交易卡片**：日期 chip + 摘要 + 分录行（账户等宽 / 金额按借贷正负着色）+ 元数据标签（type / owner / 自定义字段）。

### 3.3 视图块

- **finance-log**（`src/codeblock/log.ts`）
  倒序时间线流水；顶部筛选 tab（全部 / 收入 / 支出 / 草稿）；统计随显示条数实时重算，草稿不计入收支；自动识别并排除结转分录。
  代码块级筛选（全部选填，多条件为「与」关系）：起始日 + 天数构成时间窗口，叠加金额范围 / 账户 / 交易类型 / 归属维度四个属性筛选；
  生效中的属性条件以 chip 形式回显在头部下方。全部筛选逻辑收敛在纯函数 `filterEntries` 中，可单测。

- **finance-budget**（`src/codeblock/budget.ts`）
  按 `FinanceConfig.budgets` 渲染预算计划卡片；支持 `day / week / month / year / custom N 日` 五种周期的日期范围推算；三档进度条；跨币种金额统一折算到基准币种。

- **finance-heatmap**（`src/codeblock/heatmap.ts`）
  分类 × 周的二维支出热力图，绿色单色梯度 + 图例。

- **finance-ficalc**（`src/codeblock/ficalc.ts`）
  交互式计算器，滑块 / 输入即时重算。录入与显示用「万」，引擎内部用「分」。勾选 `useActualSpend` 时年花费改由账本流水派生。

- **finance-fi**（`src/codeblock/fi.ts`）
  从已入账交易派生净资产、总资产、总负债、年收入、年支出、年储蓄，渲染 FI 进度条 + 指标网格 + 预计达成年数。

### 3.4 语言热切换

`registry.ts` 维护 `activeBlocks` 登记表，记录工作区中所有已挂载的 finance 代码块 DOM。
设置页切换语言后调 `rerenderAllBlocks()` 整体重渲染（Reading 与 Live Preview 通用）；
并挂 `layout-change` 钩子清理已断开的 DOM，避免内存泄漏。

---

## 四、计算引擎

### 4.1 财务自由引擎

实现文件：`src/engine/fiCalc.ts`（纯函数，6 个单测）

- `isFinanciallyFree(P, r, S)`：`S ≤ P·r` 即已自由
- `requiredPrincipal(S, r) = S / r`：所需本金
- `principalGap`：本金缺口
- `yearsToDeplete`：未达标时本金能撑几年，`n = ln(S/(S−P·r)) / ln(1+r)`
- `maxAnnualSpend(P, r) = P·r`：可持续年花费上限
- `calculateActualSpend`：从月度支出 Map 算近 N 个月年化花费，返回可用月数与数据是否充足
- `yearsToFI`：按当前净储蓄速度预计几年达成目标本金，`n = ln((target+S/r)/(P₀+S/r)) / ln(1+r)`，r=0 时退化为除法
- `currentValue`：直线法折旧，算大件资产当前公允值

### 4.2 汇率引擎

实现文件：`src/engine/fx.ts`（7 个单测）

- 汇率由用户手动维护（非实时行情），约定 `rate = 1 单位该币种折合多少基准币种`
- `buildFxRates` / `buildSymbolMap`：从 `currencies` 列表派生汇率表与符号表
- `convertToBase`：原币种 → 基准币种，`Math.round` 取整到分
- `currencySymbol`：用户自定义符号优先，回退内置表（CNY/USD/EUR/JPY/GBP/HKD/KRW/TWD），再回退代码本身

---

## 五、配置体系

### 5.1 两层配置

- **vault 内 `finance-config.json`**（用户可见、可进 Git）——由 `ConfigManager` 管理
  首次使用自动写入默认配置；加载时与 `DEFAULT_CONFIG` 合并（用户只需写差异）；解析失败回退默认不阻塞启动；内置旧版 `fxRates → currencies` 迁移。

- **Obsidian 私有 `data.json`**（插件设置）——`FinancePluginSettings`
  `ledgerPath` / `archiveLedgers` / `configPath` / `draftReminder` / `draftScan`（默认关）/ `draftScanFolders`（默认空）/ `language`

### 5.2 默认配置内容

- 账户预设 6 类：现金 💵 / 银行卡 💳 / 支付宝 📱 / 微信 💬 / 投资 📈 / 负债 📉
- 五大类枚举完整
- 归属维度预设 `{自己, 家庭}`，默认「自己」
- 基准币种 USD，预置 11 种常用币种及参考汇率
- 交易类型预设 11 项（餐饮 / 交通 / 购物 / 居住 / 娱乐 / 医疗 / 教育 / 工资 / 投资收益 / 其他收入 / 转账）
- 预算预设「餐饮 每月」一项
- FI 默认年利率 4%

### 5.3 设置页

实现文件：`src/settings/SettingsTab.ts`

置顶两项路径（配置文件路径 / 账本文件路径，均带库内路径自动补全 `PathSuggest`），
账本路径旁挂「存档管理」入口；随后是语言下拉、五个管理器入口、未入账提醒开关、
**待入账筛查开关（默认关，开启时显式出现「筛查范围（文件夹）」输入框，逗号分隔，带 `PathSuggest` 补全）**。

---

## 六、UI 弹窗

**录入表单基类** `EntryFormModal` —— 三个入口共用同一张表单（字段、校验、金额元→分、上次账户记忆），只有提交行为不同：

| 入口 | 提交后行为 |
|---|---|
| `InsertCodeBlockParamModal` | 在光标处插入 fin-beancount 块（草稿态） |
| `RecordTransactionModal` | 直接入账到账本（带 `^t-` 块引用） |
| `AppendDraftToBlockModal` | 追加一笔分录到当前 fin-beancount 块的草稿区 |

**配置管理器**（数据均落 `finance-config.json`）：

- `AccountManagerModal` —— 账户增删改，含五大类徽章与图标
- `TypeManagerModal` —— 交易类型增删改，含收支方向与自定义字段
- `OwnerManagerModal` —— 归属维度增删改 + 设为默认（名称只读，避免悬空引用）
- `CurrencyManagerModal` —— 币种与汇率增删改 + 设为默认币种
- `BudgetManagerModal` —— 预算计划增删改

**其他**：

- `InsertCodeBlockModal` —— 跨插件通用插入器主弹窗（搜索框 + 按插件分组的 Tab + 卡片列表，单插件时隐藏 Tab）
- `ArchiveManagerModal` —— 可视化并清理 `archiveLedgers`，标注文件是否存在
- `RolloverModal` —— 配置新账本路径与结转截止日
- `PathSuggest` / `VaultFolderSuggestModal` / `Confirm` —— 路径补全、文件夹模糊选择、确认弹窗

---

## 七、跨插件契约（BlockProvider）

实现文件：`src/blockProvider.ts`

- **契约**：插件实例暴露 `getBlockRegistry()`，返回「我能插哪些 block」
- **发现**：通用插入器扫描 `app.plugins.plugins`，动态合并所有实现该方法的插件——无模块级注册表、无跨插件共享状态
- **宿主策略（first-claim wins）**：每个插件 onload 时都尝试注册「插入代码块」命令，第一个抢到的成为宿主，其余跳过并只作为 provider 被合并展示
  - 只装 1 个插件 → 它自己就是宿主
  - 装 N 个 → 字母序首个当宿主，合并视图按插件分组
- 只有宿主才显示「插入代码块」ribbon，避免多插件共存时出现重复按钮

---

## 八、插件入口能力

实现文件：`src/main.ts`

- **命令**：`记一笔` / `插入代码块`（仅宿主注册） / `重建索引` / `周期结转`
- **Ribbon**：`coins` 图标 → 记一笔；`code` 图标 → 插入代码块（仅宿主显示）
- **状态栏**：开启「未入账提醒」后，每分钟刷新一次，显示「N 笔未入账」，无草稿时留空
- **事件监听**：md 文件的 `modify` / `create` → 增量索引；`delete` → 清理该文件索引条目
- **代码块处理器**：6 种全部通过依赖注入注册（`ProcessorDeps` 注入 app / indexer / 路径读取器 / 配置读取器 / 弹窗打开器）

---

## 九、国际化

实现文件：`src/i18n/`

- 零依赖实现：纯对象查找 + 简单插值，扁平 `section.key` 命名
- 中文 384 条、英文 383 条键
- 缺省回退中文；切换语言即时重渲染所有已挂载代码块 + 刷新设置页

---

## 十、测试与构建

### 测试（`npx vitest run`，80 用例全绿）

| 测试文件 | 用例数 | 覆盖内容 |
|---|---|---|
| `codeblock/log.test.ts` | 25 | finance-log 参数解析、金额表达式、日期窗口（含跨月/跨年回退）、属性筛选、ID 路径 |
| `parser/finBeancount.test.ts` | 16 | 语法解析、零和校验、元数据提取、错误定位 |
| `ledger/ledgerView.test.ts` | 10 | 账本展示名派生 |
| `engine/fx.test.ts` | 7 | 汇率换算、符号回退 |
| `engine/fiCalc.test.ts` | 6 | FI 公式 |
| `ledger/indexer.test.ts` | 9 | 路径归一防重复索引、幂等更新、纯内存化、遗留缓存清理、统计口径、**待入账筛查默认关/范围过滤/留空全库** |
| `ledger/ledgerFile.test.ts` | 5 | 账本单块追加 |
| `ledger/rollover.test.ts` | 2 | 余额承接 |

### 构建

- `npm run dev` —— watch 模式，内联 sourcemap
- `npm run build` —— 生产压缩
- 输出：`版本/main.js` + 自动拷贝 `manifest.json`、`styles.css`
- esbuild 配置：CJS 格式、`external: ['obsidian']`、target ES2018、treeShaking、`.svg` 以文本内联
- 类型校验独立执行：`npx tsc --noEmit`（build 脚本本身不跑 tsc）

---

## 十一、变更记录

### 2026-07-31 · finance-log 筛选能力增强（date / amount / account / owner）

**动机**：原来的流水视图只能「看最近 N 天」，锚点永远是今天，也无法按金额、账户、归属维度收窄。
想复盘「7 月 15 号那几天在现金账户上超过 100 块的支出」只能靠肉眼翻。

**新增参数**（全部选填，缺省即不筛选，多条件为「与」关系）：

| 参数 | 语义 |
|---|---|
| `date` | **起始日**——窗口里最新的一天，缺省为今天 |
| `day` | 从起始日往前数 N 天（含起始日）。`date: 2026-07-15` + `day: 3` → 命中 07-13 / 07-14 / 07-15 |
| `amount` | 按金额**绝对值**筛选，单位元。支持 `>100` / `>=100` / `<50` / `<=50` / `100-200` / `100~200` / `100` |
| `account` | 任一分录账户命中即算（子串匹配、忽略大小写） |
| `owner` | 按归属维度筛选 |

设计决策（含代价）：

1. **`date` 是起始日而非截止日**——按用户口径「从哪天开始往前数」。语义上它是窗口的上界，因此起始日之后的账目会被排除，不会把未来账混进历史窗口。
2. **严格比较折算成闭区间**——金额内部是整数分，`>100` 无损等价于 `min = 10001 分`。下游只需一套 `min/max` 比较，不必维护 exclusive 标志位。
3. **表达式无法识别时降级为「不筛选」**，而不是「全部隐藏」——筛错字不该让用户以为账没了。同理非法 `date` 也回落到今天。
4. **`id` 与日期窗口互斥**：给了 ID 说明用户要看指定那几笔，不该再被时间窗口裁掉；但属性筛选在两条路径上都生效，语义一致。
5. **金额口径与展示口径强制统一**：抽出 `entryAmount()`，筛选和卡片右侧显示金额共用，杜绝「筛出来的数和显示的数对不上」。
6. **筛选逻辑全部收敛进纯函数 `filterEntries`**，渲染层只负责画——因此能用 25 个单测覆盖，而不必起 DOM。
7. **UI 回显**：头部 pill 反映时间窗口（`7-15 起近 3 天`），下方 chip 行回显金额/账户/类型/归属；命中为空时给的是「没有符合筛选条件的账目」而非「暂无交易记录」，避免误判成数据丢了。

**插入弹窗改造**：

- finance-log 参数顺序改为 `date → day → amount → account → type → owner → id`（起始日第一）
- 金额改为**引导式 UI**：新增 `type: 'amount'`，弹窗内渲染「运算符下拉（大于 / 大于等于 / 小于 / 小于等于 / 之间 / 等于）+ 数字框」；选「之间」时显示两个框，组合结果写入隐藏 input 还原成 `>100` / `100-200` 这类表达式，与 `parseAmountFilter` 对齐。用户不再需要手敲符号
- 账户 / 交易类型 / 归属维度改为**下拉框**，选项从 `finance-config.json` 实时读取
- 为此引入 `optionsFrom` 显式声明数据源，替换原先「按 `p.key === 'owner' | 'txnType'` 猜」的隐式约定——换个键名就失效的老写法在 `account` / `type` 上会直接哑火
- 引入 `autoToday`：日期字段是否自动填今天。fin-beancount 记账要（保持原行为），finance-log 筛选**刻意留空**，否则滚动窗口会被写死成插入当天
- 修掉一个潜在踩雷点：`collectValues` 原来只要 `key === 'amount'` 就做元→分换算，会把筛选表达式 `>100` 变成 `NaN`。现加上 `type === 'number'` 限定

**改动文件**：`codeblock/log.ts`（重写筛选层）、`util/date.ts`（新增 `daysBefore` / `isDateStr`）、`codeBlockDefs.ts`、`ui/EntryFormModal.ts`、`blockProvider.ts`、`main.ts`、`i18n/zh.ts` + `en.ts`（新增 20 条键）、`styles.css`（chip 样式）、新增 `codeblock/log.test.ts`。

### 2026-07-31 · 索引层瘦身：去掉 finance-index.json

**动机**：审查发现落盘索引缓存收益为零且有害。

问题（均为读码实证）：

1. **陈旧数据 bug**——`init()` 若发现缓存文件存在就**完全不重扫**，插件关闭期间在外部改动的笔记读不到
2. **mtime 增量形同虚设**——`files[]` 元数据只在跨会话时才可能命中，而 `modify` 事件必然使 mtime 变化，等于永不命中
3. **草稿从不记元数据**——`scanDrafts` 压根不往 `files[]` 写，增量机制本身就是残缺的
4. **污染 vault**——多一个机器生成的大 JSON，文件树与 Git diff 都是噪音

**处置**：

- 删除：`loadIndex` / `saveIndex` / `getIndexPath` / `INDEX_VERSION` / `IndexData` / `FileIndexMeta` / `lastUpdated`
- 删除零调用点的死代码：`getDrafts` / `getTransactionsByDateRange` / `getTransactionsByCategory` / `getLastUpdated`
- 删除整个死模块 `src/ledger/reader.ts`（70 行，全项目零 import，且基于已废弃的「账本文件夹」概念）
- 新增：init 时自动删除旧版本遗留的 `finance-index.json`
- 结果：**vault 内只剩 `finance-config.json` 一个插件文件**

**代价**：每次启动全量扫一遍 md（走 `cachedRead`，通常几百毫秒），换来索引永远与磁盘一致。

新增 3 个回归测试：不再写出缓存文件、遗留文件被清理、统计口径正确区分已入账与草稿。

### 2026-07-31 · 待入账筛查改为默认关（零全库扫描）

**动机**：审查「启动全量扫 md」时发现，全库遍历只为了找散落在笔记里的未入账草稿块（供状态栏「N 笔未入账」与 finance-log 草稿 tab 使用）；账本读取本就按 `ledgerPath` 直读，无需扫描。多数用法是「记一笔」直接入账，库里没有散落草稿，默认开启全库扫描是净开销。

**处置**：

- 新增设置项 `draftScan`（默认 `false`）与 `draftScanFolders`（默认 `[]`），落在 `FinancePluginSettings`（私有 `data.json`）
- `Indexer` 构造函数接受 `getDraftScan: () => DraftScanConfig` getter，从 `main.ts` 实时注入；`scanDrafts` 在 `enabled=false` 时直接 `return`，**关闭时启动只读账本、零全库遍历**
- 开启且 `folders` 非空时只扫命中的文件夹（`isInDraftScope` 前缀匹配，留空=全库）；`isLedgerFile` 保证账本永不误判为草稿
- 设置页：待入账筛查开关 +（仅开启时显示）逗号分隔的「筛查范围（文件夹）」输入框，均带 `PathSuggest` 补全；切换开关或范围即时触发 `indexer.fullScan()` 重建索引
- i18n：新增 `settings.draftScan` / `settings.draftScanFolders` 中英文键
- 新增 3 个回归测试：默认关 draftCount=0、限定范围只扫范围内、留空扫全库

**设计点（已同步进方案）**：默认不扫描全库；待入账筛查作为可选开关，开启后才按范围扫描笔记草稿；关闭时启动只读取配置的账本文件，避免无谓的全库遍历。

### 2026-07-31 · finance-fi 并入 finance-ficalc 并重设计（参考 ficalc.app）

**动机**：`finance-fi`（财务自由进度仪表）与 `finance-ficalc`（财务自由计算器）共用同一套 `fiCalc` 公式，唯一差异是「参数从账本来还是手填」——属于重复实现。把"数据源"收敛成一个分段开关，四项重复指标合并，进度条直接并进来，省一个代码块。

**合并方式**：
- `finance-fi` 的账本派生逻辑（`computeFIData`：净值/资产/负债/年花费/年净储蓄/可撑月数）原样并入 `ficalc.ts` 的 `computeLedgerSnapshot`，由 `source: actual | manual` 分段开关切换；存量笔记里的 ` ```finance-fi ` 代码块仍由 `registry.ts` 的同名**兼容别名**渲染（指向 `renderFICalc`），仅从插入器移除避免新用户再选到。
- 删除 `codeblock/fi.ts`（逻辑已并入）。

**重设计（参考 ficalc.app 的信息架构）**：参数 → 结论横幅（头部带**成功率 pill**）→ 蒙特卡洛模拟结果（成功率仪表 + P10/P50/P90 分位走势 SVG + 关键年份表），比旧的"自由/不自由"二元结论信息量大得多。

**保留并放宽的招牌三滑条**（用户明确要求）：

| 滑条 | 下限 | 上限 | 曲线 |
|---|---|---|---|
| 年利率 % | **0** | **200** | 线性（power 1） |
| 本金 万 | 0 | 100000 | 幂次 3（低值区更精细） |
| 年花费 万 | 0 | 10000 | 幂次 3 |

幂次曲线 `pos^power` 在不牺牲低值手感的前提下把量程拉大；填充式轨道用 `--fc-pos` CSS 变量驱动，拖动经 `requestAnimationFrame` 节流保证 60fps。

**引擎新增纯函数**（不依赖 vault/账本）：
- `realRate(nom, infl) = (1+nom)/(1+infl)-1`——全部按"今天购买力"计价，与 ficalc.app 一致
- `projectPortfolio(plan, returns?)`：逐年推演（年初支取 → 剩余生息 → 年末余额），支持 `fixed` / `percent` / `rule95` 三种提取策略
- `annuitySpend(P, r, n)`：n 年内恰好花光（die-with-zero）的年支取上限
- `simulateRetirement(plan, vol, runs=400, seed=固定)`：**种子化**蒙特卡洛，实际收益率按 N(realRate, vol) 抽样，同参数必给同结果（否则每次重渲染数字乱跳）；`vol=0` 退化为确定性单跑，成功率非 0 即 1

**i18n / 样式**：新增约 55 条中英文键（`param.source/savings/inflation/years/volatility/strategy` 及 `ficalc.*` 全套）；`styles.css` 补齐 `.fc-source` 分段开关、`.fi-track` 进度条、`.fc-gauge` 成功率仪表、`.fc-chart` SVG 走势图、`.fc-table` 年份表，跟随 Obsidian 主题明暗自适应。

**测试**：`fiCalc.test.ts` 新增 12 个用例（realRate / projectPortfolio 三策略 / annuitySpend / simulateRetirement 可复现+分位有序+无波动确定性），全量 **92 测试通过**，`tsc --noEmit` 干净，`npm run build` 通过。

**改动文件**：`codeblock/fi.ts`（删）、`codeblock/ficalc.ts`（重写）、`codeblock/registry.ts`、`codeBlockDefs.ts`、`engine/fiCalc.ts`、`i18n/zh.ts` + `en.ts`、`styles.css`、`engine/fiCalc.test.ts`。

### 2026-07-31（补）finance-ficalc 收尾调整

1. **移除 finance-fi 兼容别名**：用户确认不需要兼容，故 `registry.ts` 中 `finance-fi → renderFICalc` 的别名整段删除，`codeBlockDefs.ts` 注释同步更正。存量笔记里老的 ` ```finance-fi ` 代码块将不再渲染（需手动改为 ` ```finance-ficalc `）。`finance-ficalc` 的 `source: actual` 即等价于原 finance-fi 的账本派生视图。

2. **蒙特卡洛模拟改为按需计算**：成功率 pill 从头部移入模拟面板；模拟结果区默认折叠，收束为一个 `fc-sim-toggle` 展开按钮（带提示"点击展开成功率与分位走势（展开后才计算）"）。`simOpen=false` 时不调用 `simulateRetirement`，确定性结论（进度条 + 横幅 + 指标网格）始终可见；点击展开即 `schedule()` 计算并随滑条实时重算，收起即 `empty()` 清空。避免每次拖动都跑 300 次随机模拟。

**改动文件**：`codeblock/ficalc.ts`、`codeblock/registry.ts`、`codeBlockDefs.ts`、`i18n/zh.ts` + `en.ts`、`styles.css`。

### 2026-07-31（补2）finance-ficalc 两项修复

1. **滑块值重渲染丢失** → 改为持久化保存。
   - 内存缓存：模块级 `paramCache`（key = `sourcePath::source`），每次 `recalc()` 先把当前滑块值写入缓存；重渲染时 `renderFICalc` 开头读缓存覆盖 `parseParams` 的初值，语言切换/编辑↔预览切换不丢。
   - 写回源文本：`renderFICalc` 新增 `app` 参数（registry 传入 `deps.app`）；用户改动参数后防抖 800ms 调 `writeParamsToFile`，按 `source: actual|manual` 重写 `rate/principal/spend/savings/inflation/years/volatility/strategy` 行（actual 模式不写 principal/spend/savings，避免用账本快照污染源文本），保留其它注释与自定义行。真正关闭 vault 也不丢。
   - 注意：`scheduleWriteBack` 只在用户触发 `schedule()` 时调用（初始渲染与展开模拟的 `recalc()` 不写回），避免无谓改动文件。

2. **展开蒙特卡洛按钮无效** → 根因是 CSS 只写了 `.fc-sim.is-collapsed { display:none }` 却没加展开态覆盖规则，且 JS 点击时未移除 `is-collapsed`。修复：点击处理器里 `simEl.toggleClass('is-collapsed', !simOpen)` 直接移掉隐藏类；并补 `.fc-sim-wrap.is-open .fc-sim { display:block }` 规则双保险。

**改动文件**：`codeblock/ficalc.ts`、`codeblock/registry.ts`、`styles.css`。

### 2026-07-31（补3）finance-ficalc 拖拽回缩 + 参数键名缩短

1. **拖动滑条一秒后面板回缩（bug 修复）**：根因是写回源文本（`app.vault.modify`）会触发 Obsidian 自动重渲染本代码块，而 `simOpen` 是纯局部变量默认 `false`，重渲染后必然折叠；且旧缓存键 `path::source` 会因写回改变 source 文本而失效，缓存根本没命中。
   - 缓存键改为 **stableKey**：去掉所有受管参数行（仅对注释/自定义行取签名），写回不改键 → 重渲染命中同一缓存。
   - `PersistedParams` 新增 `simOpen` 字段；`persist()` 写入、`renderFICalc` 开头 `let simOpen = cached?.simOpen ?? false` 恢复；点击处理器里也 `persist()`；重渲染后在 DOM 创建后按 `simOpen` 重新展开并填充模拟面板。现在拖动滑条写回后，展开的模拟界面**不再回缩**。

2. **参数键名缩短 + 插入弹窗可配**：
   - 全部 9 个参数历来已在 `codeBlockDefs.ts` 注册，`mode`（原 strategy）本就是 `type: 'select'` 下拉框，三策略 `fixed/percent/rule95` 带中文选项标签——无需额外改造。
   - 应要求把键名缩短：`source→src`、`savings→save`、`inflation→infl`、`volatility→vol`、`strategy→mode`（`rate/principal/spend/years` 本就短，保留）。同步改 `parseParams`、`writeParamsToFile`、顶部文档注释、`codeBlockDefs` 的 `key`。
   - 兼容性：`parseParams` 同时识别新旧别名（如 `inflation`/`infl`、`strategy`/`mode`），写回只生成短键且 strip 正则会清掉所有别名行，旧笔记即使写过长键也不会残留。插入弹窗生成的代码块即 `src: ... / rate: ... / infl: ... / vol: ... / mode: ...`。

**改动文件**：`codeblock/ficalc.ts`、`codeBlockDefs.ts`、`开发日志.md`。
**验证**：`tsc --noEmit` 干净、`npm run build` 通过、全量 **92 测试通过**。

### 2026-07-31（补4）finance-ficalc 拖动回缩根治 + 移除数据源开关

1. **拖动滑条一秒后整页回初始状态（彻底修复）**：前几轮试图用「写回源文本 + 缓存恢复 simOpen」来止血，但依赖 Obsidian 在 `app.vault.modify` 后正确重渲染并恢复 UI 状态是脆弱的（用户实测仍回缩）。本轮从根上切断触发链——**彻底移除写回源文本逻辑**（`writeParamsToFile` / `scheduleWriteBack` / `renderFICalc` 的 `app` 参数一并删除，registry 不再传 `deps.app`）。拖动滑条只更新内存缓存 + `localStorage`，**不再触发任何文件改动与重渲染**，因此不会有"一秒后回缩"。
   - 持久化改用内存缓存 + `localStorage` 双写（key = `fb-ficalc:<sourcePath>::<块签名>`）：切语言、编辑↔预览切换、甚至重开 vault 都不丢。`simOpen` 同样持久化，重渲染后按缓存恢复展开/折叠。
   - 取舍：值不再写进代码块源文本（无法靠 git 跟踪、移动笔记不跟随），换取零重渲染、界面稳定。用户如需"值落在源文本"可后续再加回（届时需解决重渲染回缩）。

2. **移除 `实际账本 / 假设手填` 数据源开关**：用户反馈不理解且不需要。该开关是早期合并 finance-fi 时引入的（`source: actual` 从账本取净值/花费/储蓄，`manual` 用滑条手填）。现 `finance-ficalc` 定位为纯 what-if 沙盒（对齐 ficalc.app 的"假设式"体验），一律手填，故移除开关与对应 UI（`fc-source` 分段控件、`syncSource` / `applySource`）、删除账本派生函数 `computeLedgerSnapshot`（含 `LedgerSnapshot` 接口）及其 `Indexer` / `convertToBase` / `buildFxRates` / `calculateActualSpend` 等依赖导入；`codeBlockDefs.ts` 移除 `src` 参数项。账本联动如日后需要可独立开关加回。

**改动文件**：`codeblock/ficalc.ts`、`codeblock/registry.ts`、`codeBlockDefs.ts`、`开发日志.md`。
**验证**：`tsc --noEmit` 干净、`npm run build` 通过、全量 **92 测试通过**。

### 2026-07-31（补5）finance-ficalc 加回「保存参数」按钮（显式写回源文本）

用户要求参数值仍落到代码块里，但不要自动写回（自动写回触发 Obsidian 重渲染导致回缩）。故改为：**仅在点击「保存参数」按钮时才把当前值写回代码块源文本**；其余时候（拖动/改值/展开模拟）一律不碰文件，只走内存缓存 + `localStorage`。

1. **`writeParamsToFile` 重新引入**，但**唯一调用点是 Save 按钮的 click 处理器**。改用 `ctx.getSectionInfo(el)` 取代码块行号（开/闭围栏）做精确替换内层 body，比补4前用的"精确文本匹配整块"更稳——**连续多次保存也能命中**（旧写法在文件被改写后 `source` 不再等于文件块文本，会静默跳过）。
2. Save 处理器先 `persist()`（落内存+`localStorage`），再 `writeParamsToFile`：即便写回触发的重渲染先于文件落盘完成，缓存也已就绪，重渲染后滑块值与 `simOpen` 均从缓存恢复，不会回缩。写回保留原块注释/自定义行，只重写受管参数行（`rate/principal/spend/save/infl/years/vol/mode`），统一用短键。
3. 头部新增「保存参数」按钮（`.fb-save-btn`，`--text-on-accent` 取色）；无对应笔记文件（`app` 为空，如画布）时按钮 `disabled` 并提示"仅在笔记文件中可保存"。点击后短暂显示"已保存 ✓"反馈。
4. i18n 新增 `ficalc.save` / `ficalc.save.hint` / `ficalc.saved` / `ficalc.save.noFile`（中英文）。`renderFICalc` 重新接收 `app` 参数，registry 重新传 `deps.app`。

**改动文件**：`codeblock/ficalc.ts`、`codeblock/registry.ts`、`i18n/zh.ts` + `en.ts`、`styles.css`、`开发日志.md`。
**验证**：`tsc --noEmit` 干净、`npm run build` 通过、全量 **92 测试通过**。

### 2026-08-01 · finance-ficalc 升级阶段一：改名「现金流计算器」+ 积累期引擎 + 双线生命周期图

**动机**：用户要把 `finance-ficalc` 从「财务自由目标计算器」升级为「现金流计算器」——核心是补上现引擎缺失的**积累期**（只有退休支取半段），并给出一条能回答"我几岁财务自由"的生命周期曲线。阶段二（资产分桶接入）、阶段三（事件模拟器）留待验收后开工。

**改名（仅 UI 文案，不改围栏名）**：围栏 ` ```finance-ficalc ` 标题从「财务自由计算器」→「现金流计算器」（zh）/「Cash Flow Simulator」(en)，描述同步改为"投影长期资产与现金流，模拟人生事件冲击"。i18n 改动落在 `block.finance-ficalc` 与 `ficalc.title` 两处，中英文各补约 10 条键（`param.age`/`retireAge`/`incomeGrowth`、`ficalc.metric.fiAge`、`ficalc.lifecycle.*` 含 svg aria / 起点 / 退休 / 读数模板）。存量块零兼容风险（围栏名没变）。

**引擎新增纯函数 `projectLifeCashflow(input): LifeProjection`**（不依赖 vault）：
- 输入端 `LifePlanInput`：`currentAge`/`retireAge`/`endAge`/`principal`/`annualSavings`/`incomeGrowth`/`nominalRate`/`inflation`/`retireSpend`/`strategy`/`events?`。
- 真实收益率复用以有的 `realRate(nom, infl) = (1+nom)/(1+infl)-1`，全部按"今天购买力"计价。
- **积累期**（age < retireAge）：年初 `netWorth *= (1+rr)`，叠加当年净储蓄 `curSavings`，`curSavings *= (1+incomeGrowth)` 逐年增长。
- **退休期**（age ≥ retireAge）：复用 `projectPortfolio` 三策略（`fixed`/`percent`/`rule95`）的支取逻辑，年末余额 `max(0, after + after*rr)`。
- 逐年产出 `LifeYearPoint`（`age/year/netWorth/passiveIncome/safeCashflow/free`），收集成 `points[]`；算 `fiAge`（余量 `safeCashflow ≥ 0` 且**持续到最后一年**的拐点——自由=现金流能一直撑住）、`fiYear`、`retireIndex`。
- 导出接口 `LifeEvent` / `LifePlanInput` / `LifeYearPoint` / `LifeProjection`，为阶段三事件叠加与阶段二账本派生留好纯函数入口。

**代码块渲染（`codeblock/ficalc.ts`）**：
- `MANAGED_PARAM` 正则补 `age|retireAge|incomeGrowth`；`PersistedParams` / `FICalcParams` 新增三字段（非可选，避免 `fmtParam` 类型错）。
- `parseParams` 默认 `age:30 / retireAge:60 / incomeGrowth:0`，从缓存恢复。
- UI 加三条滑条：**当前年龄** 18–80、**退休年龄** 30–90、**收入增长** 0–20%（紧凑型）；`recalc()` 算 `endAge = max(retireAge+1, retireAge+years)` 后调引擎，再 `renderLifeCycle` 画双线图；`renderConclusion` 增「自由里程碑」卡（显示 FI 年龄，未达成显"当前参数下未达财务自由"）。
- 新增 `renderLifeCycle(parent, proj, symbol)`：SVG **双线图**——净资产水位线（绿 `.fc-net`）+ 可持续现金流余量线（琥珀 `.fc-flow`），退休虚线、FI 里程碑绿虚线、hover 读数（净资产/余量两点）。
- 参数写回沿用阶段内的「仅保存按钮」套路（`writeParamsToFile` 写三参数 + persist/save/load + 各滑条 onChange 绑定）。

**参数注册（`codeBlockDefs.ts`）**：`finance-ficalc` 块在 `rate` 后、`principal` 前补 `age`/`retireAge`/`incomeGrowth` 三个 `type:'number'` 参数（placeholder 30/60/3 + labelKey/descKey）。**已核实 `main.ts` 的 `getBlockDefinitions()` 从 `FINANCE_CODE_BLOCK_DEFS` 自动映射参数，故"三处同步"实际只需改 `codeBlockDefs.ts` 一处**（blockProvider/main 无需手改）。

**样式（`styles.css`）**：补 `.fc-lifecycle-svg` / `.fc-net`(var(--fb-green)) / `.fc-flow`(var(--fb-amber)) / `.fc-retire-line` / `.fc-fi-line` / `.fc-dot-net` / `.fc-dot-flow`，全用主题 token 自适应明暗。

**测试**：`fiCalc.test.ts` 新增 `describe('projectLifeCashflow 生命周期投影')` **7 测**——点数+积累增长 / 退休高支取净资下降 / 收入增长抬升积累终点 / 储蓄充足→FI 里程碑且余量持续≥0 / rate=0 线性 / 已自由→fiAge=currentAge / fiYear 与 fiAge 一致。全量 **99 测试通过**（fiCalc 25）。

**踩坑（已修）**：
1. `zh.ts` 并行编辑撞 linter → 拆「块级改名」与「param 级」两步重放。
2. `incomeGrowth` 在 `PersistedParams` 为可选致 `fmtParam` 类型错 → 三字段改非可选。
3. 单测断言写反（well-funded 退休期净资本就增长）→ 改用高支取场景（`retireSpend: 200_000_00`）验证"净资下降"。

**改动文件**：`codeblock/ficalc.ts`、`codeBlockDefs.ts`、`engine/fiCalc.ts`、`i18n/zh.ts` + `en.ts`、`styles.css`、`engine/fiCalc.test.ts`、`现金流计算器升级方案.md`（方案落盘）。
**验证**：`tsc --noEmit` 干净、`npm run build` 通过（版本/main.js 310463B + styles.css 53190B）、全量 **99 测试通过**。

**待办**：阶段二（`AccountDef.cashflowRole` 资产分桶 + `src: actual` 接入 + 应急金/现金利率参数）、阶段三（`EventDef` + `EventManagerModal` + 基准 vs 含事件双曲线对比）待用户验收阶段一后开工。

---

### 2026-08-01 · finance-ficalc 升级阶段二（资产接入 + 双线图图例）

**动机**：阶段一已落盘「假设式 what-if 沙盒」。阶段二目标：① 自动从账本资产账户派生本金/年花费/年储蓄预填，减少手填；② 给双线生命周期图补图例，明确绿线（净资产水位）与琥珀线（现金流余量）语义。

**关键设计决策（绕开方案 md 与开发日志补4 的冲突）**：方案 md 写阶段二要恢复 `src: actual` 显式开关，但开发日志补4 用户曾主动删除「实际账本/假设手填」数据源开关（理由「不理解且不需要」，定位纯沙盒）。→ 采用「自动检测 + 滑条覆盖」而非显式开关：检测到 `finance-config.json` 有 `class: asset` 账户即自动分桶派生本金/花费/储蓄预填滑条（用户拖动即覆盖，无需开关）；无资产账户退化为纯手填。块头显示「数据源：资产账户 / 假设（手填）」徽章。若日后想要显式开关可一句话加回。

**改动点**：
- 域模型：`types/index.ts` 的 `AccountDef` 加 `cashflowRole?`（growth/cash/fixed/rental），存 JSON、不碰 beancount 账本语法 → 满足第 8 节 beancount 合规硬约束。
- 分桶：`engine/assetBuckets.ts`（新建，纯函数）：`bucketAssets(config, balances, valuations, owner?, {annualSpend, bufferMonths})` 按 `cashflowRole`（缺省按 valuation 智能推断：depreciation→fixed、market→growth、book/未设→cash；liability 恒进负债桶）分桶，输出 growthValue/cashValue/fixedValue/rentalValue/liabilities/emergencyBuffer/interestPrincipal/cashAboveBuffer/nonInterestAssets/netWorth。应急金缓冲 = 年支出 × 月数 / 12（不增长）。
- 引擎：`engine/fiCalc.ts` 的 `LifePlanInput` 加可选 `cashRate`/`cashPrincipal`/`nonInterestAssets`/`liabilities`；`projectLifeCashflow` 重写积累/支取循环：生息本金拆 growth/cash 两段分别按 `rr`/`cashRate` 复利，退休期按两桶比例分摊支取；netWorth = 生息本金 + 非生息资产 − 负债。**默认值下行为与阶段一完全一致**（不传则不拆桶、现金桶=0、净资产=本金），故未破坏既有 30 个 fiCalc 测试。
- 渲染：`codeblock/ficalc.ts`：① `renderFICalc` 签名加 `indexer?`/`ledgerPath?`；② 资产派生（近 12 月实际年花费/年净储蓄年化 + `bucketAssets` 分桶）自动预填 principal/spend/savings 滑条初值；③ 资产模式专属参数「现金收益率 / 应急金月数」滑条（仅资产模式显，`.fc-asset-params.is-hidden` 控制）；④ 数据源徽章（`.fc-source-badge`）；⑤ `recalc` 用派生值喂引擎；⑥ 持久化补 `cashRate`/`bufferMonths`/`source`，写回源文本仅资产模式落 `source: actual` + 两参数；⑦ `renderLifeCycle` 顶部加图例（绿=净资产水位、琥珀=现金流余量）。
- `codeBlockDefs.ts` 加 `cashRate`(默认 1.5) / `bufferMonths`(默认 6) 两个 number 参数（`main.ts` 自动映射，无需手改）。
- `i18n/zh.ts` + `en.ts`：`modal.accounts.cashflowRole*`、`param.cashRate*`、`param.bufferMonths*`、`ficalc.source.assets`、`ficalc.lifecycle.legendNet`/`legendFlow` 等。
- `AccountManagerModal.ts`：账户编辑加 `cashflowRole` 字段 UI + 列表徽章；`class` 非 asset 时隐藏该字段。
- `registry.ts`：`finance-ficalc` 调用点补传 `deps.indexer, deps.getLedgerPath()`。
- `styles.css`：`.fc-legend*`、`.fc-source-badge`、`.fc-asset-params(.is-hidden)`。

**验证**：`tsc --noEmit` 干净、`npm run build` 通过、**全量 109 测试通过**（新增 `assetBuckets.test.ts` 5 例 + `fiCalc.test.ts` 资产分桶 5 例；继承阶段一 99 例）。

**改动文件**：`types/index.ts`、`engine/assetBuckets.ts`(新)、`engine/fiCalc.ts`、`codeblock/ficalc.ts`、`codeBlockDefs.ts`、`registry.ts`、`ui/AccountManagerModal.ts`、`i18n/zh.ts` + `en.ts`、`styles.css`、`engine/assetBuckets.test.ts`(新)、`engine/fiCalc.test.ts`、`现金流计算器升级方案.md`。

**待办**：阶段三（`EventDef` 人生事件 + `EventManagerModal` + 基准 vs 含事件双曲线对比）待用户验收阶段二后开工。

### 2026-08-01（补）· 双线图图例说明 + 资产模式可发现性修复
- **背景**：用户验收阶段二时提出三问——(1) 双线图两条线为何画在一起、琥珀为何在绿线上方；(2) 顶部「假设（手填）」徽章含义；(3) 插入弹窗写入的 `cashRate`/`bufferMonths` 在视图里找不到编辑框、且没有「进入资产模式」的按钮。
- **结论/修复**：
  1. 双线图澄清：绿线（净资产水位）占满全高、0 在底部；琥珀线（现金流余量）以图正中为 0、正负上下分置——**两套纵轴/零线叠加**，故「琥珀在绿上方」是坐标系错觉，不代表余量>净资产。图例（阶段二已加）只标语义，未解释叠加。→ 已向用户说明，并提议三种重设计方案（A 保留叠加+标注双轴 / B 拆成上下两块独立子图 / C 只留现金流余量线，净资产交给进度条），待用户拍板，未改图。
  2. 「假设（手填）」徽章 = 数据源状态标签（手填沙盒 vs 资产账户派生），非按钮。已加 tooltip 说明；手填模式下额外显示「配置资产账户 → 自动启用」提示。
  3. **真实缺口修复**（ficalc.ts / styles.css / i18n）：① `cashRate`/`bufferMonths` 原被 `is-hidden` 在手填模式隐藏 → 改为**始终显示+始终写回**，下方加说明「仅资产模式参与计算，手填模式仅作预设」；满足「插入的每个参数在视图都有编辑框」。② 原 `source: manual` 会被写回代码块并缓存进 localStorage，导致「配置账户后自动切换」承诺失效 → 改为**模式纯由 `config.accounts` 是否含 asset 类自动判定**，不再写回/缓存 `source`，配置资产即自动切「资产账户」模式。
- **改动文件**：`codeblock/ficalc.ts`、`i18n/zh.ts`+`en.ts`、`styles.css`（新增 `.fc-mode-hint`/`.fc-asset-hint`，删除 `.fc-asset-params.is-hidden`）。
- **验证**：`tsc --noEmit` 干净；`vitest` 109 测全过；`npm run build` 成功。
- **遗留待定**：双线图是否重构（见上 A/B/C）待用户决策；资产模式自动切换后需笔记重渲染才生效（改 finance-config.json 不会自动触发块重渲染，需编辑笔记或切预览）。

**待办**：阶段三（事件模拟器）待用户验收阶段二后开工；双线图重构方案待用户选定。

---

### 2026-08-01（补2）· 现金流模拟器：改名 + 去冗余参数 + 去模式区分

**动机**：用户验收三层图后提出三点改进：(1) 标题「现金流计算器」→「现金流模拟器」更贴切；(2) 参数太多且有重复——「退休年龄」出现两次（主区 `retireAge`=绝对年龄 vs 更多假设里 `years`=退休年限，标签撞了），本金/年花费/年净储蓄可从资产派生不需手填，大量参数有合理默认值不该占用主区；(3) 「模拟」/「资产账户」模式区分无意义——有资产就自动预填、没资产就手填，用户永远可以覆盖任何值，不需要一个模式徽章来提示。

**改动点**：
- **i18n（zh+en）**：`block.finance-ficalc` / `ficalc.title`「现金流计算器」→**「现金流模拟器」**；`param.years` 从错误标签「退休年龄」/ "Retirement age" 修正为**「预测年限**" / "Projection years"`（desc 同步改为说明自动计算至 95 岁）；`ficalc.assetParams.hint` 去掉「仅在资产账户模式下」的模式相关表述。
- **ficalc.ts 渲染重构**：
  - **删除** `sourceBadge`（「模拟」/「资产账户」徽章）和 `modeHint`（「配置资产账户 → 自动启用」提示）——不再有模式区分 UI。
  - 参数布局从「5 个滑条平铺主区 + 更多假设 7 个」重构为三层：
    - **`.fc-core`**（必须手填，带淡背景色区分）：当前年龄(18–80) / 退休年龄(30–90) / 年利率(0–200%) —— 仅这 3 个无法从数据派生。
    - **`.fc-derived`**（三列网格，可编辑）：本金 / 年花费 / 年净储蓄 —— 有资产账户时自动分桶/账本派生预填，无则显示默认值；用户可随时覆盖。
    - **`.fc-more` details（折叠不变）**：通胀率 / 预测年限（**默认自动算 `max(30,95-退休年龄)`**）/ 波动率 / 储蓄增长 / 现金收益率 / 应急金月数 / 提取策略。
  - 修复 **`yearsSlider` 重复 bug**：从更多假设移除重复的 `savingsSlider`（已移至 `.fc-derived` 行）；`years` 默认值改用 `autoYears` 而非硬编码 30。
  - 内部仍保留 `assetMode` 逻辑（分桶派生引擎入参），仅去掉视觉模式标记。
- **styles.css**：新增 `.fc-core`(flex 列 + 淡背景块) / `.fc-derived`(grid 三列 + 窄屏自适应单列)；`.fc-source-badge` / `.fc-mode-hint` 样式保留但已成死代码（无元素引用，无害）。

**验证**：`tsc --noEmit` 干净；`vitest` **109 测全过**；`npm run build` 成功（版本/main.js + styles.css）。

**改动文件**：`codeblock/ficalc.ts`、`i18n/zh.ts`+`en.ts`、`styles.css`。

---

### 2026-08-01 · finance-ficalc 双线图重做：三层生命周期图 + 蒙特卡洛 8 卡 + 事件层埋点

**动机**：阶段二「补」遗留待定——用户否决了叠加双线图（绿净资产水位 + 琥珀现金流余量共用 XY 造成"琥珀压绿"错觉、两层语义混叠），要求重构为**三层结构**：① 净资产曲线（线）② 现金流柱状图（正绿负红，零分位在层正中）③ 关键事件层（竖线贯穿三层 + 菱形标记 + 交替上下标签防重叠）。三层共用同一条 x 轴（年龄）。原型的「假设（手填）」徽章改「模拟」、去除杜撰的「运行模拟」按钮、蒙特卡洛改可折叠、图表内灰色图层说明文字全删（改由底部图例传达）。经 `redesign/ficalc-block-3layer-prototype.html` 多轮迭代（v4 对照真实界面纠偏 + v5 红框竖排图层标签 / 退休年龄 / 蒙特卡洛 8 卡）确认后，落地进真实源码。

**改动点**：
- i18n（`zh.ts` + `en.ts`）：`ficalc.source.manual`「假设（手填）」→**「模拟」**（hint/cta 同步）；`param.years`「退休年限」→**「退休年龄」**；新增 lifecycle 图层与图例键 `lifecycle.layerNet`(净资产曲线)/`layerFlow`(现金流)/`layerEvent`(关键事件)/`zeroLine`(零分位)/`retireEvent`(退休)/`eventNote`(📝)/`legendFlow`(现金流为正)/`legendNeg`(现金流为负)/`legendEvent`(关键事件（链接笔记）)，`lifecycle.aria` 扩充。
- 样式（`styles.css`）：新增设计 token `--fb-blue: #2f6fed; --fb-blue-soft: rgba(47,111,237,0.14);`；**删除旧双线类**（`.fc-lifecycle-svg`/`.fc-net`绿/`.fc-flow`琥珀/`.fc-retire-line`/`.fc-fi-line`/`.fc-dot-*`），替换为三层一套：`.fc-lc3`(flex 横排) / `.fc-layer-labels`+`.fc-layer-label`(竖排 `writing-mode: vertical-rl`，不挤图内) / `.fc-net`(蓝描边+淡蓝面积) / `.fc-bar-pos`(绿)/`.fc-bar-neg`(红)/`.fc-zero-line`(中轴实线) / `.fc-event-line`+`.fc-diamond`+`.fc-event-label`+`.fc-event-leader`(琥珀贯穿竖线+旋转菱形+交替标签+引线) / `.fc-legend-dot.net`(蓝)/`.pos`(绿)/`.neg`(红)/`.event`(琥珀菱形) / `.fc-sim-metrics`(4 列网格承载蒙特卡洛 8 卡)。
- 渲染（`codeblock/ficalc.ts`）：
  - `renderLifeCycle(parent, proj, symbol, events?: LifeEvent[], onEventClick?: (ev: LifeEvent)=>void)` **整体重写为三层 SVG**（viewBox 680×316）：净资层(16–120) 蓝线+淡蓝面积；现金流层(140–252，零分位中线 y=196) 绿/红柱（正负分置中线上下）；事件层(266–300) 贯穿竖线 + 旋转菱形（事件年龄处）+ 交替上下标签（`k%2`）+ 引线；FI 里程碑紫虚线（`var(--fb-purple,#7c5cff)`）穿过三层；左侧竖排 `.fc-layer-labels` 三层名；x 轴年龄刻度。
  - `renderSimulation(...)` 扩展 `extra` 入参（`target/gap/rr/fiAge/fiYear/principal/savings/age/spend`），蒙特卡洛区由旧 `fc-stats` 改为 **`.fc-sim-metrics` 8 卡网格**——所需本金/本金缺口/可持续年花费/安全提取率/可撑年数/预计达成年份/年净储蓄/自由里程碑，全部复用既有 `ficalc.metric.requiredPrincipal/gap/maxSpend/swr/years/reach/savings/fiAge` 键（未新增 metric 键）。**删除** 已失效的 `simStat()` 死函数。
  - 新增 `LifeEvent` 接口（`id`/`type: retire|house|child|windfall|custom`/`label`/`age`/`year?`/`note?`）——阶段三事件模拟器的数据载体，本块先内建一个"退休"事件并埋点：`recalc()` 内 `events=[{id:'retire', type:'retire', label:t('ficalc.lifecycle.retireEvent'), age: retireAge, year: 当前年+max(0,retireAge-age)}]`；`onEventClick` 调 `app.workspace.openLinkText(ev.note, ctx.sourcePath, false)`（note=Obsidian wikilink，阶段三 editEvent 弹窗埋点已留位）。
  - 数据源徽章已用 `t('ficalc.source.manual')`，落地后自动显示「模拟」。
- 原型：`redesign/ficalc-block-3layer-prototype.html`（v5，已与真实界面结构对齐，仅图表区为三层替换）。

**验证**：`tsc --noEmit` 干净；`vitest` **109 测全过**（三层图为纯渲染、引擎逻辑复用既有 30 个 fiCalc 测护航，未新增渲染单测）；`npm run build` 成功（版本/main.js + styles.css）。

**改动文件**：`codeblock/ficalc.ts`、`i18n/zh.ts` + `en.ts`、`styles.css`、`redesign/ficalc-block-3layer-prototype.html`。

**待办**：阶段三完整事件模拟器（`EventDef` + `EventManagerModal` + `editEvent` 弹窗 + 用户事件替换内建"退休"事件）待开工；事件层点击 `openLinkText` 埋点已就位，目前仅内建退休事件 + `note` 链接可点。

---

### 2026-08-01（补3）· 现金流模拟器 UI 微调：派生值双形态 + 年净储蓄入更多假设 + 本金改名

**动机**：用户验收（补2）后提出三点细化：(1) 派生值（本金/年花费/年净储蓄）应同时支持「默认派生 + 滑条手填覆盖」两种形态——已有此能力，本次明确固化；(2) 年净储蓄挪进「更多假设」折叠区，主派生行只留最该一眼看到的两个；(3) 「本金」改称「生息本金」，语义更准（退休引擎只吃生息类资产）。

**改动点**：
- **i18n（zh+en）**：`param.principal`「本金」→**「生息本金**"（en: "Interest-bearing principal"`），desc 同步强调"可用于退休支取的生息类资产本金"。
- **ficalc.ts 布局**：
  - `.fc-derived` 派生行从「本金/年花费/年净储蓄」三列收窄为**「生息本金 / 年花费」两列**（仍派生默认 + 滑条覆盖双形态）。
  - `savingsSlider` 从派生行**移入 `.fc-more` 的 `advGrid`**（紧跟预测年限之后），保留"派生默认 + 手填覆盖"双形态。
  - 注：点 (1) 要求的"双形态"本就是现状（派生值写进 `initial.*` 喂给滑条 `value`，用户拖动即覆盖），本次仅按点 (2)(3) 调整位置与标签，能力不变。
- **styles.css**：`.fc-derived` 网格 `1fr 1fr 1fr` → **`1fr 1fr`**（适配两列）。

**验证**：`tsc --noEmit` 干净；`vitest` **109 测全过**；`npm run build` 成功（版本/main.js + styles.css）。

**改动文件**：`codeblock/ficalc.ts`、`i18n/zh.ts`+`en.ts`、`styles.css`。

---

### 2026-08-01 finance-ficalc：派生滑条并入核心框 + 移除「耗尽式年提取」卡片

用户两点：(1) 生息本金/年花费不要同行，应在年利率下方逐行排列、与核心参数**同框**；(2) 删掉「{n} 年耗尽式年提取」结果卡片。

**改动点**：
- **ficalc.ts 布局**：`.fc-derived` 容器的挂载点由 `root` 改为 **`core`**（即 `.fc-core` 框内），紧随 `rateSlider` 之后，生息本金与年花费各占一行。派生值的 muted label 视觉标识保留（靠 `.fc-derived .fc-label` 选择器）。
- **ficalc.ts 结果区**：删除 `renderResult` 中的 `ficalc.metric.burnDown` 卡片（原 8 卡 → 7 卡），同步删除已失效的 `annuitySpend` import（引擎函数 `fiCalc.ts` 保留，单测仍覆盖）。
- **i18n（zh+en）**：删除 `ficalc.metric.burnDown` / `.desc` 两键（zh/en 同步，保持键对齐）。
- **styles.css**：`.fc-derived` 由 `grid 1fr 1fr` 改为 **`flex column; gap: 0.6em`**，与 `.fc-core` 内部行距一致；移除 `margin-bottom` 与 420px 断点（同框后不再需要）。

**验证**：`tsc --noEmit` 干净；`vitest` **109 测全过**；`npm run build` 成功（版本/main.js + styles.css，12:39 刷新）。

**改动文件**：`codeblock/ficalc.ts`、`i18n/zh.ts`+`en.ts`、`styles.css`。

---

### 2026-08-01 · finance-ficalc 阶段三：人生事件模拟器（完整落地）

**动机**：阶段一/二已具备完整生命周期投影（积累期 + 退休期）与三层图（净资曲线 / 现金流柱 / 事件层占位）。事件层此前只内建一个"退休"假事件作占位埋点（`retire` 竖线）。阶段三目标：让用户定义真实的"买房 / 生娃 / 结婚 / 意外之财 / 职业变动 / 自定义"人生事件，分别作用到现金流引擎（改积累/支取轨迹）与图表事件层（按类型着色的竖线 + 菱形 + 交替标签），并支持点击笔记链接或点开编辑弹窗。

**关键设计决策**：
1. **三类数据分离、单一真源**：`finance-config.json` 里的 `lifeEvents: LifeEventDef[]` 是唯一真源。一条 `LifeEventDef` 在运行时被**展开成两种形态**：① `EngineLifeEvent`（喂 `projectLifeCashflow`，仅财务影响字段：`atAge/oneOff/deltaSpend/deltaIncome/deltaFixed/deltaLiability`）② `ChartEvent`（画事件层，仅展示字段：`id/type/label/age/year/note`）。二者由同一条 Def 派生，杜绝重复存储与字段漂移。
2. **故意保留两个命名空间、别名隔离**：引擎侧 `fiCalc.ts` 的 `LifeEvent`（财务语义）与渲染侧旧 `ficalc.ts` 的 `LifeEvent`（展示语义：retire/house/child…）撞名。本次把渲染类型**改名为 `ChartEvent`**，引擎导入**起别名 `EngineLifeEvent`**——彻底隔离"算什么"与"画什么"，互不污染。
3. **`lifeEvents` 走浅合并、零迁移**：`ConfigManager.merge` 已对 `finance-config.json` 做浅合并，`FinanceConfig` 加必填 `lifeEvents` 后旧配置自动 fallback 到 `DEFAULT_CONFIG` 的 `[]`，无需任何迁移代码。`defaults.ts` 在 `fiCalc:` 前插入 `lifeEvents: []`。
4. **事件过滤语义**：`recalc()` 只取 `config.lifeEvents` 中 `enabled && age ∈ [currentAge, endAge]` 的事件参与；开关（`enabled`）实现"what-if 但不删"，关掉即退出本次投影。
5. **事件着色靠 CSS 变量继承**：每个 `.fc-event[data-event-type=...]` 只 override 一个 `--ev-c` token；竖线 / 菱形 / 标签 / 引线全部 `var(--ev-c)` 取色。图表层与弹窗徽章共用同一组 type→color 映射，视觉始终同步、CSS 极少。
6. **点击行为分层**：事件标记 hover 出 `<title>` 提示（有 note 显示"点击打开笔记"，无 note 显示"点击编辑事件"）；点击时——有 `note` 走 `app.workspace.openLinkText(note, sourcePath, false)` 打开 Obsidian 链接，否则（含内建 retire 事件）调 `openLifeEventModal(() => recalc())` 重算。

**改动点**：
- `types/index.ts`：新增 `LifeEventType = 'house'|'child'|'marriage'|'windfall'|'career'|'custom'`、`LifeEventDef`（id/label/type/age/enabled/note? + 五个财务影响字段 `oneOff`/`deltaSpend`/`deltaIncome`/`deltaFixed`/`deltaLiability`，单位均为分）；`FinanceConfig` 加 `lifeEvents: LifeEventDef[]`。
- `config/defaults.ts`：在 `fiCalc:` 前加 `lifeEvents: []`（旧配置合并安全）。
- `src/ui/LifeEventManagerModal.ts`（新）：`LifeEventManagerModal`（列表 + 新增 / 编辑 / 删除 / 启用开关）+ 内嵌 `LifeEventEditModal`（表单）。`EVENT_TYPES` 常量数组驱动类型下拉；`IMPACT_FIELDS` 常量数组驱动 5 个财务影响输入；note 字段用 `PathSuggest` 补 vault 路径。`impactSummary()` 拼"一次性 -¥80万 · 负债 +¥240万"式摘要。编辑弹窗保存时元→分换算；先删遗留影响键再重赋，避免脏值残留。`onChanged` 回调在开关/删除/编辑关闭时触发以重算。
- `src/codeblock/registry.ts`：`ProcessorDeps` 加 `openLifeEventModal: (onChanged?: () => void) => void`；finance-ficalc 调用点补传 `deps.openLifeEventModal`。
- `src/main.ts`：import `LifeEventManagerModal`；deps 加 `openLifeEventModal: (onChanged) => this.openLifeEventModal(onChanged)`；新增私有方法 `openLifeEventModal(onChanged?)` 实例化并 `open()`。
- `src/codeblock/ficalc.ts`：
  - import `type LifeEvent as EngineLifeEvent` from `../engine/fiCalc`；`LifeEventType` from `../types`。
  - `renderFICalc` 签名加 `openLifeEventModal?`。
  - 头部新增「事件」按钮（`.fb-save-btn.fc-event-btn`，title=事件提示），点击调 `openLifeEventModal(() => recalc())`。
  - 渲染接口 `LifeEvent` → `ChartEvent`（`events: ChartEvent[]`、`onEventClick?: (ev: ChartEvent) => void`）。
  - `recalc()` 内：过滤 `userEvents` → 映射 `engineEvents: EngineLifeEvent[]` 喂 `projectLifeCashflow({..., events})`；`events: ChartEvent[]` 由内建 `retire` + `userEvents.map(...)` 组成并 `sort(age)`；`onEventClick` 分层（note→openLinkText / 否则→openLifeEventModal）。
  - 事件标记加 hover `<title>`（`svgEl(g, 'title', {})`），文本用 `ficalc.lifecycle.eventTipNote` / `eventTipEdit`。
- `i18n/zh.ts` + `en.ts`：新增 `modal.event.*`（title/intro/add/noData/editTitle/newTitle/label/type/age/atAge/hasNote/enabled/disabled/enabledLabel/impactSection/impactHint/oneOff(+desc)/deltaSpend(+desc)/deltaIncome(+desc)/deltaFixed(+desc)/deltaLiability(+desc)/note(+placeholder)/labelRequired/ageRequired/impactInvalid/confirmDelete/deleted/saved）、`event.type.{house,child,marriage,windfall,career,custom}`、`ficalc.events`/`ficalc.events.hint`/`ficalc.lifecycle.eventTipNote`/`ficalc.lifecycle.eventTipEdit`。
- `styles.css`：新增 token `--fb-teal:#14a3a3; --fb-teal-soft; --fb-pink:#e0507a; --fb-pink-soft`；事件层重写用 `--ev-c` 变量（`.fc-event{--ev-c:var(--fb-amber)}` + 按 type 覆盖：house→蓝 / child→teal / marriage→pink / windfall→绿 / career→紫），line/diamond/label/leader 全用 `var(--ev-c)`，hover 提亮；弹窗样式 `.fb-event-card.is-disabled`（半透明+删除线）、`.fb-action-btn.is-off`（虚线）、`.fb-badge.ev-*`（类型色）、`.fb-subhead`、`.fb-check-row`。

**测试**：`engine/fiCalc.test.ts` 新增 `describe('projectLifeCashflow 人生事件叠加（阶段三）')` **10 测**——空事件=无影响 / oneOff 在触发年生效 / 触发前无影响 / deltaFixed（净资产升、被动不变）/ deltaLiability（净资产降、被动不变）/ deltaSpend（积累变慢 + FI 延后）/ deltaIncome（积累变快 + FI 提前）/ 买房组合（净平但被动降）/ 多年龄事件 / 同年龄多事件。全量 **119 测试通过（9 文件）**（fiCalc 40）。

**踩坑（已修）**：
- `assetBuckets.test.ts` 的 `makeConfig` 缺 `lifeEvents` → tsc 报必填字段缺失，补 `lifeEvents: []`。
- `type: 'retire' as const` 缺失致数组字面量把 `'retire'` 拓宽成 `string`、与 `LifeEventType|'retire'` 不兼容 → 加 `as const`。
- `svgEl(g, 'title')` 缺第三参（attrs）报错 → 改 `svgEl(g, 'title', {})`。

**改动文件**：`types/index.ts`、`config/defaults.ts`、`src/ui/LifeEventManagerModal.ts`(新)、`src/codeblock/registry.ts`、`src/main.ts`、`src/codeblock/ficalc.ts`、`i18n/zh.ts`+`en.ts`、`styles.css`、`engine/fiCalc.test.ts`、`src/engine/assetBuckets.test.ts`。
**验证**：`tsc --noEmit` 干净；`npm run build` 成功（版本/main.js 350055B + styles.css 59783B）；全量 **119 测试通过（9 文件）**。

**待办**：阶段三已完整落地，事件模拟器可用。后续可选项：① 事件层"基准 vs 含事件"双曲线对比（当前为单曲线 + 事件标记，未做对比叠加）；② 事件与年金/负债联动细化（如房贷月供自动喂 `deltaSpend`）。

---

### 2026-08-01（补）· 事件管理入口缺失修复：设置页 + 命令面板双入口

**问题（用户实测反馈）**：阶段三把事件管理入口只挂在 `finance-ficalc` 代码块头部的「事件」按钮上（`ficalc.ts` 的 `fb-actions` 区，与「保存参数」并排）。用户在设置页 / 命令面板里找不到入口，误以为功能没落地。其他五类管理器（币种/账户/类型/归属/预算）在设置页都有「打开管理」按钮，唯独人生事件缺失，一致性断裂 + 不可发现。

**修复（补齐与既有管理器的对等入口）**：
- `src/settings/SettingsTab.ts`：import `LifeEventManagerModal`，在「预算管理」之后加一条 `Setting`——名称 `settings.lifeEventManager`（"人生事件"）、描述 `settings.lifeEventManager.desc`、按钮 `settings.openManager`（"打开管理"）→ `new LifeEventManagerModal(this.plugin).open()`。与预算管理器同构。
- `src/main.ts`：在「更新资产估值」命令后加 `addCommand({ id: 'finance-life-events', name: t('command.lifeEvents'), callback: () => this.openLifeEventModal() })`——命令面板可直接唤出事件管理器（无需先打开任意 `finance-ficalc` 块）。
- `i18n/zh.ts`+`en.ts`：新增 `settings.lifeEventManager` / `settings.lifeEventManager.desc` / `command.lifeEvents`（中英文对齐）。

**说明**：块头部「事件」按钮仍保留（在现金流图上点标记/直接在此块内增删事件最顺手），现在形成三处一致入口——设置页、命令面板、块头部。事件数据全局唯一（`finance-config.json` 的 `lifeEvents`），三处打开的是同一个管理器，改动即时落盘并触发 `onChanged` 重算所有 `finance-ficalc` 块。

**改动文件**：`src/settings/SettingsTab.ts`、`src/main.ts`、`i18n/zh.ts`+`en.ts`。

### 2026-08-01（补3）· UI 微调：基本参数折叠 + 图例 + 图层标签

1. **基本参数可折叠**：当前年龄/起始年龄/退休年龄/年利率/生息本金/年花费 6 滑条并入 `<details class="fc-basic">` 折叠（默认展开），同「更多假设」的 ▸/▾ 交互。
2. **图例文案精简 + 排序**：「净资产水位」→「净资产」、「历史（记账确定）」→「历史」；排序改为历史→净资产→现金流正→负→事件。
3. **图层侧标签对齐**：左竖排 `space-around` → `space-between`，文字对齐图表三层。

**改动文件**：`codeblock/ficalc.ts`、`i18n/zh.ts`+`en.ts`、`styles.css`。

### 2026-08-01（补4）· startAge 参数 + 历史段（记账数据驱动图表起始段）

**动机**：用户要求图表从「起始年龄」开始（如 18 岁），startAge→currentAge 用真实记账数据填充（已确定信息），currentAge→endAge 仍用模拟引擎。可调参数基于当前年龄不变。

**设计**：
- `startAge` 参数（默认 = `age`，即图表从当前年龄开始，向后兼容）。
- `startAge < age` 时：图左侧多出一段"历史柱"——半透明蓝色柱形，数据来自实际 beancount 账本的净资产累加。
- 模拟段一切照旧（蓝色面积+曲线 / 绿红现金流柱），但只占右侧区域。
- 「当前」虚线（灰色 dash）标记历史与模拟的分界。

**改动点**：
- `codeBlockDefs.ts`：`startAge` 参数。
- `ficalc.ts` 参数链：`FICalcParams`/`PersistedParams`/`parseParams`/cache/writeBack/persist/UI 滑条。
- `computeHistoricalNetWorth()`（新函数，~50 行）：遍历已入账交易，按 Income/Expense 分录贝算符号累加净资变动；逐年输出 `HistoricalPoint[]`（startAge→currentAge）。
- `renderLifeCycle` 重构为合并数据集模式：`MergedPoint{age,netWorth,safeCashflow,isHistorical}`；历史段画 `.fc-hist-bar` 柱、模拟段面积+曲线；现金流仅模拟段；x 轴含 startAge/今/退休/终点。
- i18n 新增 `param.startAge`/`lifecycle.legendHist`/`now`/`nowShort`/`readoutHist`；styles 新增 `.fc-hist-bar`/`.fc-now-line`。

**边界**：历史数据仅在 assetMode 且有 indexer（已入账交易）时计算；无账本数据自动退化为 startAge=age（无历史段，纯模拟）。

**改动文件**：`codeBlockDefs.ts`、`codeblock/ficalc.ts`、`i18n/zh.ts`+`en.ts`、`styles.css`。

**验证**：tsc 零报错、119 测全过、build 成功。

### 2026-08-01（补5）· 退休事件化 + 图表事件层点击/重叠修复

**动机**：用户两条反馈：(1) 退休硬编码在 `recalc()` 里、不可管理——不符合"所有人生事件统一入口"的设计；(2) `finance-ficalc` 图表里的关键事件标签堆叠、不可点击（光标呈十字星，无法打开 Obsidian 笔记链接）。

**退休事件化**：
- `LifeEventType` 首位加 `'retire'`；`LifeEventDef.age` 注释 retire 时忽略（位置由 ficalc 退休年龄参数驱动）。
- `defaults.ts` 的 `lifeEvents` 种子化：`[{ id:'retire', label:'退休', type:'retire', age:60, enabled:true }]`（旧配置经 `ensureRetire()` 幂等回填）。
- `LifeEventManagerModal`：`onOpen` 调 `ensureRetire()` 回填旧配置；`renderEvents` 删除按钮 guarded `if (ev.type !== 'retire')`——退休事件不可删除；`LifeEventEditModal` 对 retire 禁用 type/age 字段 + 隐藏财务影响表格 + 显示 `retireAgeHint`。
- `recalc()` 过滤 userEvents 时排除 `type==='retire'`（不喂引擎），retireDef 取自 config.lifeEvents（enabled 显 / disabled 不画）；`onEventClick` 统一全部打开管理器。
- `styles.css`：`[data-event-type='retire'] { --ev-c: var(--fb-amber); }` + `.fc-event-hit`。

**图表事件层修复**：
- **根因**：全屏透明 `fc-hit` rect（`cursor: crosshair`）绘制在事件 `<g>` **后面**，拦截所有事件层的点击与光标。
- **修复**：`fc-hit` → `pointer-events: none`（纯绘制层）；mousemove 从 `hit` 移到 `svg`；每个事件产出透明可点击区 `.fc-event-hit` rect（`pointer-events:all, cursor:pointer`）——点击菱形/竖线/标签均可打开笔记，光标正常 pointer。
- **标签重叠**：旧 `k%2` 交替+labelY(311) 与 x 轴刻度(310) 碰撞 → 贪婪方向分层算法：同侧间距 < 46vw 时上跳 tier（12px 纵向），上/下各四档；SVG 高度 316 → 344 留空间；每点引线（菱形→标签）+ `<title>`。

**编译修补（三处残留错误）**：① `ficalc.ts:1368` forEach→for loop 遗留 `});` → `}`；② `ficalc.ts:1408` 生命周期函数删除 hit 变量后残留 `hit.addEventListener('mouseleave')` → `svg`；③ `LifeEventManagerModal.ts` 补 `isRetire` 字段声明 + onOpen 赋值 + save 的 age 从 config 取。

**改动文件**：`types/index.ts`、`config/defaults.ts`、`ui/LifeEventManagerModal.ts`、`codeblock/ficalc.ts`、`i18n/zh.ts`+`en.ts`、`styles.css`。

**验证**：tsc 零报错、119 测全过、build 成功。临时脚本已清理。
**验证**：`tsc --noEmit` 干净；`npm run build` 成功；新构建 `版本/main.js` 含 `settings.lifeEventManager` / `command.lifeEvents` / `finance-life-events` 三处入口。
