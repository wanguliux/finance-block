# 日常花费辅助记账 · 功能设计方案（定稿）

> 状态：已拍板定稿（2026-08-03 评审，结合插件源码核对）｜ 原型见同目录 `日常花费辅助记账-原型.html`

## 一句话定位

为**高频、规律、低变动**的日常支出（地铁通勤、订阅费、会员卡等）提供「**设一次计划 → 每天自动出草稿 → 一键入账**」的轻量闭环，把「每天重复手输」变成「每天点一下」。

## 要解决的问题

1. **重复劳动** —— 规律支出每次都走完整录入流程。
2. **易遗漏** —— 没有待办提示，哪天没记就漏了。
3. **难补齐** —— 多天没开笔记，想一次性补很麻烦。

## 用户故事

- 设定一次「地铁通勤」计划，之后每天只需点一下入账，不用重复填表。
- 看到「今天还有几条没入账」，不遗漏。
- 金额偶尔可调（某天打车）：**仅本次覆盖**，下次草稿仍按计划金额。
- 某天不需要（休假）可跳过，账本不被脏数据污染。

## 功能范围（MVP）

- 计划**新建 / 编辑 / 暂停 / 软删**（`active:false`，误删可恢复）。
- 虚派生「今日待入账草稿」（非真实文件草稿；打开笔记的任意一天自动补齐，含逾期）。
- 一键入账（直接写账本）、跳过、改金额（仅本次覆盖）、批量入账（批量前弹一次确认）。
- 新代码块 `finance-recurring` 作为唯一展示 / 操作界面。

## 数据模型（写入 finance-config.json）

`recurringPlans[]` + `recurringSkips`，遵守 vault 内单文件原则：

```json
{
  "id": "plan-subway",
  "name": "地铁通勤",
  "amount": 600,              // 整数分；MVP 为单金额两腿，多腿模板（如房贷本息拆分）为预留扩展
  "account": "日常",           // 支出账户（必填）
  "fromAccount": "现金",       // 出资账户（必填）
  "txnType": "支出",
  "owner": "自己",
  "frequency": "weekday",      // daily | weekday | monthly
  "monthlyDay": 1,             // frequency=monthly 时生效（1–28）
  "startDate": "2026-08-01",
  "endDate": "",               // 可选，留空=长期有效
  "note": "上下班地铁",
  "active": true
}
```

`recurringSkips`：`{ "plan-subway": ["2026-08-03"] }`

## 草稿派生（虚派生）

- 渲染时对每个 `active` 计划，按 `frequency` 从 `startDate` 算到今天（含），得到「应发生日期集合」。
- 已入账识别：入账分录写 `plan` / `plan-date` 元数据（解析器自动落入 `txn.fields`）；渲染器按 `fields.plan` / `fields['plan-date']` 过滤 `getPostedTransactions()`。
- 未入账且不在 skip 列表 → 渲染为待入账草稿卡。
- 无需后台定时任务。

## 界面（finance-recurring 代码块）

沿用现有 `.finance-block` 设计系统，源文本无必填参数（` ```finance-recurring``` `）：

1. **`.fb-head`**：图标 `repeat` + 标题「日常花费」+ 状态 pill（「今日 N 条待入账」/「已全部入账 ✓」）。
2. **今日待入账**（可折叠；**逾期与今日平铺展示**）：草稿卡 = 计划名 + 日期 + 金额（可点改）+ 分录预览（只读）+ `[入账] [跳过] [改金额]`；区块头 `[全部入账]`（直接落账，批量前弹一次确认）。
3. **我的计划**：每条显示 名称 / 频率 / 金额 / 下次发生日 + `[编辑] [暂停] [删除]`。
4. **`[+ 新建计划]`** → 打开 `RecurringPlanModal`。

**空状态**：无计划 → 「还没有计划，点此新建」；全清 → 「今天没有待入账的花费 🎉」。

## 入账落盘

1. 组装 beancount 文本（**日期默认 = 应发生日**，逾期补录时账本记应发生日，弹窗可改）：

   ```
   2026-08-03 * 地铁通勤
     日常  600
     现金  -600
     plan: plan-subway
     plan-date: 2026-08-03
   ```

2. 追加到账本：复用 `ledgerFile.appendEntryToLedgerBlock(app, ledgerPath, entryBody, refId)`（已存在，纯追加、不动笔记）。
3. `indexer.updateFile(ledgerPath)` 后**主动重渲染本块**（Obsidian 代码块不会因账本变化自动重绘）。

**ProcessorDeps 新增（main.ts 注入）**：`openRecurringPlanModal` / `postRecurringEntry` / `skipRecurringEntry`。

## 频率模型

| 频率 | 含义 | 适用场景 |
|---|---|---|
| `daily` | 每天 | 日常小额零散支出 |
| `weekday` | 周一至周五 | 通勤（主场景；不含法定节假日） |
| `monthly` | 每月第 N 日（1–28） | 订阅 / 房租 / 会员 |

## 计划弹窗字段（RecurringPlanModal，控件级复用 EntryFormModal）

| 字段 | 类型 | 说明 |
|---|---|---|
| 名称 | text | 必填，全局唯一 |
| 金额 | number（元） | 内部转整数分 |
| 支出账户 | select（accounts） | 必填 |
| 出资账户 | select（accounts） | 必填 |
| 分类 | select（transactionTypes） | 必填 |
| 归属 | select（owners） | 缺省「自己」 |
| 频率 | select | daily / weekday / monthly |
| 每月几号 | number | 仅 monthly 显示（1–28） |
| 起始日 | date | 必填 |
| 结束日 | date | 可选 |
| 备注 | text | 可选 |

## 已拍板决策（2026-08-03）

- 草稿**虚派生**；入账**直接落账**（卡片已显示预览），批量前弹一次确认。
- 入账日期默认**应发生日**（弹窗可改）；改金额 = **仅本次覆盖**（写 plan / plan-date 元数据，不记覆盖历史）；逾期**平铺**展示。
- 支出 / 出资账户**均必填**，不设默认机制（功能本质即「先填好省得重复填」）。
- 频率 MVP 三种（数据模型为 `weekly + weekdays[]` 预留扩展位，向后兼容）；计划存 `finance-config.json`。
- 状态栏与 indexer 草稿数**分开计数**（「草稿 N · 待入账 M」；recurring 虚派生待办不进 indexer）。
- 计划 → 预算联动（txnType 同源，budget 视图加「预计 vs 实际」对比线）**进 roadmap**。

## 落地顺序

1. config schema 加 `recurringPlans` / `recurringSkips` + 类型定义。
2. 渲染器按 `txn.fields.plan` / `fields['plan-date']` 过滤 `getPostedTransactions()`，**不加** indexer 专用 API。
3. 入账复用 `appendEntryToLedgerBlock`，无需新原语；`ProcessorDeps` 增补三个方法（main.ts）。
4. 写 `finance-recurring` 渲染器（head / 草稿卡 / 计划列表 / 空状态）；入账/跳过/改金额后主动重渲染本块。
5. 新建 `RecurringPlanModal`（控件级复用，monthly → 「每月几号」联动自绘）。
6. 状态栏扩展（「草稿 N · 待入账 M」）。

---

## V2 · 贷款类计划（loan）

把「房贷」抽象为**贷款类**：设一次贷款（本金 / 年利率 / 年限 / 还款方式），引擎按还款计划逐期派生待入账草稿，一键入账时自动生成 3 腿分录（出资 / 负债 / 利息）。

### 与 V1 recurring 的关系

- 独立数据 `loanPlans[]`（LoanDef），**不合进 recurringPlans**：LoanDef 由还款引擎驱动（schedule 计算），字段语义与固定金额完全不同，合并会让 recurringPlans 膨胀成瑞士军刀。
- **共享 V1 骨架**：同一 finance-recurring 块、「今日待入账」并列展示、「我的贷款」独立 section；入账复用 `appendEntryToLedgerBlock`；虚派生逻辑抽公共骨架。
- 幂等识别用 `loan` + `loan-period`（**期号**，比日期稳——日期可能因顺延/补录错位，期号唯一）。

### 数据模型（config version 4 → 5）

```json
{
  "id": "loan-mortgage",
  "name": "房贷",
  "type": "annuity",               // 还款方式：annuity 等额本息 | equal-principal 等额本金 | interest-first 先息后本
  "principal": 1000000_00,         // 贷款本金（分）
  "annualRate": 3.5,               // 年利率 %
  "termYears": 30,                 // 年限（1–50）
  "frequency": "monthly",          // 还款周期（monthly | quarterly，预留）
  "firstPaymentDate": "2026-09-01",// 首期还款日
  "assetAccount": "银行卡",         // 出资账户（asset）
  "liabilityAccount": "房贷",       // 负债账户（liability）
  "interestAccount": "利息",        // 利息费用账户（expense）
  "txnType": "房贷",
  "owner": "自己",
  "note": "",
  "active": true
}
```

`loanSkips`：`{ "loan-mortgage": [3, 5] }`（按**期号**记录跳过）。

### 还款引擎（src/engine/loan.ts，纯函数可单测）

周期利率 r = 年利率 / 年周期数（monthly=12）；总期数 n = termYears × 周期数。

- **等额本息（annuity）**：每期总额 A = P·r(1+r)ⁿ / [(1+r)ⁿ−1]（四舍五入到分）；第 k 期利息 = 剩余本金×r，本金 = A − 利息。
- **等额本金（equal-principal）**：每期本金 = P/n；第 k 期利息 = (P − 已还本金)×r；每期总额递减。
- **先息后本（interest-first）**：前 n−1 期仅利息 P×r；末期还 P + 利息。
- **尾差原则**：本金四舍五入到分，**末期内本金 = 剩余本金**，保证本金累计精确等于贷款本金（末期总额允许与 A 差几分）。
- **续算规则（编辑 / 部分提前还款的关键）**：编辑贷款后已入账期不动（账本真源），从「下一未入账期」起以 **剩余本金 = principal − 已入账本金累计（账本派生）** 续算；「剩余本金」做成可覆盖输入 → 天然支持**部分提前还本**（改小 = 模拟提前还一笔）。

导出：`computeLoanSchedule(def): LoanPeriod[]`、`loanEntryText(period, def): string`。单测覆盖：三种方式零和、本金守恒、期号/日期序列。

### 草稿派生与幂等

- 渲染时对每个 active 贷款算 schedule → 过滤 `date ≤ 今天` 且未入账（按 `fields.loan` + `fields['loan-period']` 查账本）且不在 `loanSkips` 的期 → 草稿卡；逾期平铺（与 V1 一致）。
- 入账分录写元数据：`loan: <id>` / `loan-period: <N>` / `loan-date: <应还日>`。

### 入账分录（3 腿，符号由 legSignedCents 按账户类别推导）

```
2026-09-01 * 房贷
  银行卡  -5000        ; 资产减少
  房贷    +4100        ; 负债减少（借方），净资产回正
  利息     +900        ; 费用增加
  loan: loan-mortgage
  loan-period: 1
  loan-date: 2026-09-01
```

零和 -5000 + 4100 + 900 = 0 ✓；房贷账户余额逐期下降 → 净资产 / finance-assets / ficalc 自动正确，与 rollover / budget（利息进 txnType 分类）均无冲突。

### UI 扩展（finance-recurring 块）

- **待入账草稿卡**：贷款卡显示「房贷 · 第 12 期」+ 本期总额 + 可展开本息拆分（本金 / 利息）+ `[入账] [跳过]`；**无「改金额」**（金额由引擎决定；特殊操作走「跳过 + 手动记一笔」或「编辑贷款续算」）。
- **我的贷款 section**：每笔显示 剩余本金（principal − 已入账本金累计，账本派生）/ 期数进度（已还 N / 总 M）/ 下期金额 + `[编辑] [暂停] [删除]`。
- **LoanModal**（新建 / 编辑）：本金 / 年利率 / 年限 / 还款方式 / 首期日 / 周期 / 三账户 / 分类 / 归属；还款方式与利率变化时**实时预览首期与总额**。**编辑模式**显示「剩余本金（可覆盖）」输入框 + 提示：改小 = 模拟部分提前还本，计划从下一未入账期以新剩余本金续算。

### V2 边界

- 自动调整（利率联动重算、顺延、双周还款）不做——「编辑贷款即重算续算」已覆盖大部分现实场景。
- 结清：软删贷款；提前结清 = 「剩余本金覆盖为 0」续算末笔（还清余额）。

### V2 落地顺序

1. `engine/loan.ts`：computeLoanSchedule / loanEntryText + 单测（三方式 × 零和 / 本金守恒）。
2. config schema：loanPlans / loanSkips + 类型定义（version 4→5）。
3. 渲染器扩展：待入账并列 + 我的贷款 section + 操作后主动重渲染。
4. LoanModal（实时月供预览）。
5. 状态栏「待入账 M」并入贷款待办。
